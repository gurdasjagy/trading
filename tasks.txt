Strict instructions: Perform all the tasks in single session with proper working code for everything. Don't write any simulation or pseudo code. Everything must be perfect and should be implemented with proper planning and design.

TASTKS 1 : ( BUGS FIXES ) . 
Fix 1 : [CRITICAL] Fix SharedState /dev/shm Permission Denied - Telemetry Infinite Retry Loop
Problem

The Rust engine's telemetry thread fails to initialize the SharedState writer at /dev/shm/trading_state with Permission denied (os error 13), then retries every 500ms indefinitely, flooding logs with thousands of identical warnings. This also breaks the Python shared state reader, meaning the Python cold path has NO live market data from Rust.
Root Cause

In rust_engine/src/telemetry.rs, the ensure_init() method is called on every heartbeat (every 500ms). When SharedStateWriter::new() fails due to permissions, it retries on the next call with no backoff, no max-retry limit, and no fallback. The Docker container runs as user: "0:0" (root), but /dev/shm may have restrictive permissions or the shared_state.rs file creation uses incorrect mode flags.
Log Evidence


rust-engine-1 | WARN trading_engine::telemetry: Failed to initialize shared state writer: Permission denied (os error 13)
rust-engine-1 | WARN trading_engine::telemetry: Telemetry lazy init failed: SharedState init failed: Permission denied (os error 13)


This repeats every 500ms for the entire runtime.
Files to Fix



        
      
rust_engine/src/telemetry.rs - Add exponential backoff + max retry limit to ensure_init(). After N failures, log once and stop retrying.

        
      
rust_engine/src/shared_state.rs - Fix file creation permissions. Use OpenOptions with mode 0o666 or 0o644. Ensure the SHM path directory exists before creating the file.

        
      
crypto_trading_bot/docker-compose.yml - Verify /dev/shm volume mount has correct permissions. Add tmpfs mount with explicit size and mode if needed.

Exact Fix

telemetry.rs - Add backoff to ensure_init()


// Add fields to TelemetryPublisher:
state_init_attempts: u32,
max_state_init_attempts: u32, // e.g., 10

fn ensure_init(&mut self) {
    if self.journal.is_none() || self.state_writer.is_none() {
        if self.state_init_attempts >= self.max_state_init_attempts {
            return; // Stop retrying after max attempts
        }
        self.state_init_attempts += 1;
        if let Err(e) = self.init() {
            if self.state_init_attempts == self.max_state_init_attempts {
                warn!("Telemetry init permanently failed after {} attempts: {} — running without shared state", self.state_init_attempts, e);
            }
        }
    }
}


shared_state.rs - Fix file creation


use std::os::unix::fs::OpenOptionsExt;
let file = std::fs::OpenOptions::new()
    .read(true)
    .write(true)
    .create(true)
    .mode(0o666)
    .open(path)?;


docker-compose.yml - Add explicit tmpfs


rust-engine:
  tmpfs:
    - /dev/shm:exec,size=256m,mode=1777


Prompt for Coding Agent

Fix the SharedState permission denied error that causes infinite retry loops in the telemetry thread. In rust_engine/src/shared_state.rs, find the file creation code for the SHM path and add .mode(0o666) using std::os::unix::fs::OpenOptionsExt. Also ensure the parent directory is created with std::fs::create_dir_all() before opening the file. In rust_engine/src/telemetry.rs, add a state_init_attempts: u32 field to TelemetryPublisher, initialize it to 0 in new(), and modify ensure_init() to stop retrying after 10 failed attempts (log a final warning and return early). In crypto_trading_bot/docker-compose.yml, add a tmpfs entry under the rust-engine service: - /dev/shm:exec,size=256m,mode=1777 to ensure shared memory has correct permissions. Also apply the same permission fix in rust_engine/src/bridge_ipc/ for tick_broadcast, exec_confirm_broadcast, and any other SHM file creators that use /dev/shm/bridge_* paths.
  Fix 2 : [CRITICAL] Fix All 5 Tera Template Parse Errors - Dashboard Completely Broken
Problem

All 5 HTML dashboard templates fail to parse in the Tera template engine (Jinja2-compatible), causing the entire web dashboard to return errors. The Rust dashboard server falls back to "No template directory found" mode.
Log Evidence


rust-engine-1 | WARN trading_engine::dashboard_server: [dashboard] Failed to parse templates in /app/templates:
* Failed to parse "/app/templates/forex_trades.html" --> 78:41 (strategies.append)
* Failed to parse "/app/templates/forex_dashboard.html" --> 175:66 (pipe in conditional)
* Failed to parse "/app/templates/dashboard.html" --> 212:64 (concatenation with default filter)
* Failed to parse "/app/templates/risk.html" --> 83:58 (default with dict value)
* Failed to parse "/app/templates/settings.html" --> 160:53 (inline if without else)


Root Cause

The templates use Jinja2-specific syntax that is NOT supported by Tera:


        
      
strategies.append() - Tera has no .append() method on arrays

        
      
(value | filter) >= 0 - Tera doesn't support pipe filters inside parenthesized conditionals

        
      
(a + b) concatenation with default(value=[]) - Tera doesn't support this expression form

        
      
default(value={}) - Tera doesn't support dict literals as default values

        
      
'selected' if condition - Tera requires if/else/endif, not inline ternary without else


Files to Fix



        
      
crypto_trading_bot/templates/forex_trades.html (line 78) - Remove strategies.append() logic; pre-compute unique strategies in the Rust template context

        
      
crypto_trading_bot/templates/forex_dashboard.html (line 175) - Replace (pos.pip_pnl | default(value=0)) >= 0 with a simpler Tera-compatible conditional

        
      
crypto_trading_bot/templates/dashboard.html (line 212) - Replace (pos_symbols + (trading_pairs | default(value=[]))) with pre-computed variable in Rust context

        
      
crypto_trading_bot/templates/risk.html (line 83) - Replace default(value={}) with default(value="") or handle in Rust context

        
      
crypto_trading_bot/templates/settings.html (line 160) - Replace 'selected' if condition with {% if condition %}selected{% endif %}


Exact Fixes

forex_trades.html (line 78)

Before: {% set _ = strategies.append(trade.strategy) %}
Fix: Remove the dynamic strategy collection entirely. Instead, pass a pre-computed unique_strategies list from the Rust template_context() method in dashboard_server.rs. Replace the strategy filter dropdown with:

{% for strat in unique_strategies %}
<option value="{{ strat }}">{{ strat }}</option>
{% endfor %}


forex_dashboard.html (line 175)

Before: {% if (pos.pip_pnl | default(value=0)) >= 0 %}
Fix: {% if pos.pip_pnl is defined and pos.pip_pnl >= 0 %}
Or assign first: {% set pp = pos.pip_pnl | default(value=0) %} then {% if pp >= 0 %}
dashboard.html (line 212)

Before: {% set all_symbols = (pos_symbols + (trading_pairs | default(value=[]))) %}
Fix: Pre-compute all_symbols in the Rust template_context() method and pass it directly. Remove this line and replace with just using trading_pairs from context.
risk.html (line 83)

Before: {% set daily = risk.daily_status | default(value={}) %}
Fix: In Rust template_context(), ensure risk.daily_status is always a valid object. In the template: {% if risk.daily_status %}{% set daily = risk.daily_status %}{% else %}{% set daily = false %}{% endif %}
settings.html (line 160)

Before: {{ 'selected' if settings.trading_mode == 'paper' }}
Fix: {% if settings.trading_mode == 'paper' %}selected{% endif %}
Apply this pattern to ALL {{ 'selected' if ... }} occurrences in settings.html.
Also Fix in dashboard_server.rs

Add missing template context variables that templates expect:


        
      
risk object with daily_status, circuit_breaker, volatility_regime etc.

        
      
settings object with trading_mode, exchange, risk sub-objects

        
      
unique_strategies list

        
      
trading_pairs list

        
      
all_symbols pre-computed list

        
      
forex_pairs list

        
      
total_trades, win_rate, total_pnl for forex templates

        
      
margin_used, free_margin, daily_pnl for forex dashboard

Prompt for Coding Agent

Fix all 5 Tera template parse errors. The templates use Jinja2 syntax that Tera doesn't support. Make these exact changes:


        
      
In crypto_trading_bot/templates/settings.html: Replace ALL occurrences of {{ 'selected' if CONDITION }} with {% if CONDITION %}selected{% endif %}. There are approximately 6 such occurrences for the trading_mode and order_type selects. Also replace {{ 'checked' if CONDITION }} with {% if CONDITION %}checked{% endif %}.


        
      
In crypto_trading_bot/templates/dashboard.html: Remove line 212 ({% set all_symbols = ... %}). Replace it with {% set all_symbols = trading_pairs | default(value=[]) %}. Also remove {% set pos_symbols = positions %} if it exists before that line since positions is an array of objects, not symbols.


        
      
In crypto_trading_bot/templates/risk.html: Replace {% set daily = risk.daily_status | default(value={}) %} with {% if risk is defined and risk.daily_status is defined %}{% set daily = risk.daily_status %}{% endif %}. Then wrap all daily.X references in {% if daily is defined %} guards.


        
      
In crypto_trading_bot/templates/forex_dashboard.html: Replace all {% if (pos.pip_pnl | default(value=0)) >= 0 %} patterns with two lines: {% set pp = pos.pip_pnl | default(value=0) %} followed by {% if pp >= 0 %}. Apply the same pattern for pos.pnl_usd.


        
      
In crypto_trading_bot/templates/forex_trades.html: Remove the entire {% set strategies = [] %} / {% set _ = strategies.append(...) %} block (lines 75-80). Replace the strategy filter options with a static list or pass unique_strategies from the Rust context.


        
      
In rust_engine/src/dashboard_server.rs, in the template_context() method: Add ctx.insert("risk", &json!({"daily_status": {}, "circuit_breaker": {"triggered": false}, "volatility_regime": "normal", "drawdown_pct": 0.0, "open_positions": 0})); and ctx.insert("settings", &json!({"trading_mode": trading_mode, "exchange": {"default_leverage": 5, "max_leverage": 20, "order_type": "limit", "trading_pairs": ["BTC/USDT", "ETH/USDT"]}, "risk": {"max_position_size_pct": 10.0, "max_open_positions": 5, "max_daily_loss_pct": 2.0}})); and ctx.insert("trading_pairs", &config_symbols); and ctx.insert("signals", &Value::Array(vec![]));.
                                                                              
Fix 3 : [CRITICAL] Fix INVALID_KEY Error - Gate.io API Authentication Broken in Testnet Mode
Problem

The execution router fails to fetch the initial account balance with INVALID_KEY: Invalid key provided, meaning the circuit breaker has no baseline balance, and NO trades can be executed. The bot is completely non-functional for live trading.
Log Evidence


rust-engine-1 | WARN trading_engine: [execution] Failed to fetch initial balance: Unknown error INVALID_KEY: Invalid key provided — using defaults


Root Cause (Multiple Issues)



        
      
Contract specs fetch also fails with HTTP 502: [sizer] Contract specs fetch failed: HTTP 502 Bad Gateway - The fetch_contract_specs() function in position_sizer.rs sends authenticated requests to a PUBLIC endpoint. The testnet proxy rejects auth headers on public endpoints.

        
      
API key resolution: The engine_config.toml has api_key = "${GATEIO_API_KEY}" but the env var expansion in expand_env_vars() may not properly resolve when the variable is empty or unset, leaving literal ${GATEIO_API_KEY} as the key value.

        
      
Testnet URL mismatch: The config file has rest_url = "https://fx-api.gateio.ws/api/v4" (mainnet) but when TRADING_MODE=testnet, the code overrides to testnet URLs. However, the position sizer uses the config's base_url directly before the override happens.

        
      
is_valid_key() check: The function checks !k.starts_with("${") but doesn't check for empty strings from env vars that resolve to empty.

Files to Fix



        
      
rust_engine/src/position_sizer.rs - Remove auth headers from the public /futures/usdt/contracts endpoint call. The comment says "no authentication needed" but the code may still send headers via the shared reqwest::Client.

        
      
rust_engine/src/main.rs - Fix the position sizer initialization order: apply env overrides BEFORE fetching contract specs. Currently apply_env_overrides() runs during load_config() but the sizer init block re-reads the config's base_url which may still be mainnet.

        
      
rust_engine/src/gateio_gateway.rs - Verify the get_balance() method uses the correct testnet REST URL and proper HMAC signing for Gate.io v4 API.

        
      
rust_engine/src/main.rs - In the is_valid_key() function, also reject keys that are just whitespace.

Exact Fix

position_sizer.rs - Remove auth from public endpoint

The fetch_contract_specs function already has a comment saying auth is not needed, but verify the client.get(&url).send() call doesn't include default auth headers. Create a fresh reqwest::Client without default headers for this call.
main.rs - Fix sizer init URL


// In the position sizer init block (section 7j), use the RESOLVED base_url:
let base_url = if gw_cfg.testnet {
    "https://fx-api-testnet.gateio.ws/api/v4"
} else {
    "https://fx-api.gateio.ws/api/v4"
};


This is already correct in the code, but verify gw_cfg.testnet is true when TRADING_MODE=testnet.
main.rs - Fix is_valid_key


fn is_valid_key(k: &str) -> bool {
    !k.is_empty() && !k.starts_with("${") && k.trim().len() > 5
}


Prompt for Coding Agent

Fix the Gate.io API authentication failure. The root cause is that API keys are not being properly loaded from environment variables.


        
      
In rust_engine/src/main.rs, in the is_valid_key() function, add a check for minimum key length: !k.is_empty() && !k.starts_with("${") && k.trim().len() > 8. This prevents empty or placeholder keys from being treated as valid.


        
      
In rust_engine/src/main.rs, in the position sizer initialization block (search for "7j. Initialize Position Sizer"), add debug logging to print whether testnet mode is detected: info!("[sizer] Testnet mode: {}, base_url: {}", gw_cfg.testnet, base_url);. Also verify the gw_cfg.testnet flag is correctly set by checking the apply_env_overrides function sets ex.testnet = true for testnet mode.


        
      
In rust_engine/src/position_sizer.rs, in the fetch_contract_specs function, ensure the HTTP client does NOT send any Authorization headers. Create a plain reqwest::Client::new() inside the function instead of using the passed-in client, since the contracts endpoint is public and testnet proxies reject auth headers on public endpoints.


        
      
In rust_engine/src/gateio_gateway.rs, verify the get_balance() method constructs the correct URL for testnet (https://fx-api-testnet.gateio.ws/api/v4/futures/usdt/accounts) and that the HMAC-SHA512 signature is computed correctly per Gate.io v4 API docs (method + path + query + timestamp + body hash).   
                                                                                                              
Fix 4 :  [HIGH] Alpha Oracle Signal Queue Not Executing Trades - Pending Integration
Problem

The Alpha Oracle signal queue (Python -> SHM -> Rust) receives signals but does NOT actually execute them. The execution router logs Alpha Oracle signal accepted — pending execution pipeline integration and drops the signal. This means the entire Python confluence engine (60+ strategies) produces signals that are never traded.
Root Cause

In rust_engine/src/main.rs, the execution_router_loop() function has a TODO comment at the Alpha Oracle signal processing section:

// TODO: Convert intent to OrderCommand and submit through existing pipeline.
// For now, log and update metrics. Full integration requires:
// 1. PreTradeRiskEngine validation
// 2. Position slot acquisition
// 3. Submission via gateway
info!("[execution] Alpha Oracle signal accepted — pending execution pipeline integration");


The signal is read from the SHM queue but never converted to an OrderCommand and never submitted to the gateway.
Also Broken: Python Strategy Evaluation is Empty

In crypto_trading_bot/core/cold_path_orchestrator.py, the _evaluate_strategies_for_symbol() method returns an empty list:

async def _evaluate_strategies_for_symbol(self, symbol: str) -> list:
    # ... reads shared state ...
    # Strategy evaluation placeholder — in production, each strategy module
    # is called here with the candle/tick data and returns a StrategySignal.
    return signals  # ALWAYS EMPTY


No actual strategy logic is implemented. The 60+ strategies mentioned in comments are never called.
Files to Fix



        
      
rust_engine/src/main.rs - In execution_router_loop(), replace the TODO with actual OrderCommand construction and gateway submission

        
      
crypto_trading_bot/core/cold_path_orchestrator.py - Implement at least the core strategies in _evaluate_strategies_for_symbol()


        
      
crypto_trading_bot/core/alpha_oracle.py - Verify the signal queue producer works correctly

Exact Fix for Rust Side

In execution_router_loop(), replace the TODO block with:

// Convert TradeIntent to OrderCommand
let sym_id = registry.get_id(&intent.symbol);
if sym_id == 0 { continue; }

let is_buy = intent.side == 0; // 0=LONG, 1=SHORT
let cmd = OrderCommand {
    symbol_id: sym_id,
    side: if is_buy { spsc::side::BUY } else { spsc::side::SELL },
    order_type: spsc::order_cmd_type::LIMIT,
    leverage: intent.leverage.clamp(1, 125) as u8,
    _pad: [0; 3],
    price: FixedPrice::from_f64(intent.entry_price).raw(),
    qty: fixed_point::FixedQty::from_f64(intent.contracts as f64).raw(),
    order_id: now_ns() as u64,
    signal_ns: intent.timestamp_ns,
    max_slippage_bps: 50,
    ttl_ms: 5000,
    stop_loss_fp: FixedPrice::from_f64(intent.stop_loss).raw(),
    take_profit_fp: FixedPrice::from_f64(intent.take_profit).raw(),
    placement_type: 0,
    post_only: 0,
    _pad2: [0; 6],
};

// Validate through pre-trade risk
if let Err(rejection) = pre_trade_risk.check(&cmd) {
    warn!("[execution] Alpha Oracle rejected by risk: {:?}", rejection);
    continue;
}
if !position_slots.try_acquire() {
    warn!("[execution] Alpha Oracle: position slots full");
    continue;
}

// Push to execution ring for processing
if !exec_ring.try_push(cmd) {
    position_slots.release();
    warn!("[execution] Alpha Oracle: exec ring full");
}


Prompt for Coding Agent

Complete the Alpha Oracle signal execution pipeline. This is the most critical missing feature - signals are generated but never executed.


        
      
In rust_engine/src/main.rs, find the comment // TODO: Convert intent to OrderCommand and submit through existing pipeline. inside the execution_router_loop() function. Replace the TODO and the info! log with actual code that: (a) converts the TradeIntent fields to an OrderCommand struct, (b) runs it through pre_trade_risk.check(), (c) acquires a position slot via position_slots.try_acquire(), (d) pushes the command to exec_ring for the existing execution pipeline to process. Use the registry.get_id(&intent.symbol) to convert the symbol string to a symbol_id. Map intent.side (0=LONG, 1=SHORT) to spsc::side::BUY/spsc::side::SELL. Use intent.entry_price, intent.stop_loss, intent.take_profit for the price fields. Use intent.contracts for the quantity.


        
      
In crypto_trading_bot/core/cold_path_orchestrator.py, in the _evaluate_strategies_for_symbol() method, implement at least 5 core strategies: RSI divergence, MACD crossover, Bollinger Band squeeze, EMA crossover, and Volume-weighted momentum. Each strategy should read candle data from the shared state reader, compute its indicator, and append a StrategySignal to the signals list when a setup is detected. Use the state variable that's already read from SharedStateReader. For each strategy, set appropriate confidence, entry_price, stop_loss, and take_profit values based on the indicator calculations.

Fix 5 : [HIGH] Bridge IPC SHM Permission Denied - Tick Broadcast and Exec Confirm Broken
Problem

The Bridge IPC subsystem fails to initialize tick broadcast and execution confirmation channels due to permission denied errors on /dev/shm/bridge_ticks and /dev/shm/bridge_exec. This breaks the Rust-to-Python data pipeline for normalized tick data and fill notifications.
Log Evidence


rust-engine-1 | WARN trading_engine: Bridge: Tick broadcaster init failed (non-fatal): Failed to open SHM /dev/shm/bridge_ticks: Permission denied (os error 13)
rust-engine-1 | WARN trading_engine: Bridge: Exec confirmation init failed (non-fatal): Failed to open SHM /dev/shm/bridge_exec: Permission denied (os error 13)


Root Cause

Same underlying issue as the SharedState permission problem (Issue #9). The SHM file creation in rust_engine/src/bridge_ipc/tick_broadcast.rs and rust_engine/src/bridge_ipc/exec_confirm_broadcast.rs doesn't set proper file permissions (mode 0o666).
Files to Fix



        
      
rust_engine/src/bridge_ipc/tick_broadcast.rs - Add mode(0o666) to file creation

        
      
rust_engine/src/bridge_ipc/exec_confirm_broadcast.rs - Add mode(0o666) to file creation

        
      
rust_engine/src/bridge_ipc/portfolio_receiver.rs - Same fix for consistency

        
      All other files in rust_engine/src/bridge_ipc/ that create SHM files

Prompt for Coding Agent

Fix permission denied errors for all Bridge IPC SHM files. In every file under rust_engine/src/bridge_ipc/ that creates files in /dev/shm/, find the OpenOptions or File::create calls and add .mode(0o666) using std::os::unix::fs::OpenOptionsExt. Specifically fix tick_broadcast.rs, exec_confirm_broadcast.rs, and portfolio_receiver.rs. Also ensure std::fs::create_dir_all() is called on the parent directory before file creation. The pattern to apply everywhere is:

use std::os::unix::fs::OpenOptionsExt;
std::fs::OpenOptions::new()
    .read(true).write(true).create(true)
    .mode(0o666)
    .open(path)?;
Fix 6: [HIGH] SL/TP Orders Not Submitted to Exchange - Only Tracked Locally
Problem

Stop-loss and take-profit orders are calculated and logged but NEVER actually submitted to Gate.io as conditional/trigger orders. They are only tracked locally in the Rust engine's memory. If the engine crashes or restarts, all SL/TP protection is lost and positions are left unprotected.
Root Cause

In rust_engine/src/main.rs, the execution router has this comment after a successful fill:

// — Step 5: Submit SL/TP conditional orders —
// Note: Gate.io supports setting SL/TP via the price_trigger API
// For now, we track them locally and fire market orders if breached
if cmd.has_stop_loss() {
    info!("[execution] SL registered: {} @ {:.4}", res.order_id, ...);
}


The SL/TP are only logged, never submitted to the exchange. The ExitEvaluator checks them on each tick, but this only works while the engine is running.
Impact



        
      
Catastrophic risk: If the engine crashes, positions have NO stop-loss protection

        
      
Latency: Local SL/TP checks depend on WebSocket tick latency; exchange-side triggers execute instantly

        
      
Gaps: Price gaps during network disconnects won't trigger local SL/TP

Files to Fix



        
      
rust_engine/src/gateio_gateway.rs - Add submit_price_trigger() method using Gate.io's /futures/usdt/price_orders API

        
      
rust_engine/src/main.rs - Call the new method after successful order fill

        
      
rust_engine/src/execution_gateway.rs - Add submit_stop_loss() and submit_take_profit() to the ExecutionGateway trait

Prompt for Coding Agent

Implement exchange-side SL/TP order submission for Gate.io futures. This is critical for risk management.

In rust_engine/src/execution_gateway.rs, add two new methods to the ExecutionGateway trait:


async fn submit_stop_loss(&self, symbol: &str, side: OrderSide, trigger_price: f64, size: i64) -> Result<OrderResult, ExchangeError>;
async fn submit_take_profit(&self, symbol: &str, side: OrderSide, trigger_price: f64, size: i64) -> Result<OrderResult, ExchangeError>;




        
      
In rust_engine/src/gateio_gateway.rs, implement these methods using Gate.io's price trigger API endpoint POST /futures/usdt/price_orders. The request body should include: {"initial": {"contract": symbol, "size": size, "price": "0", "tif": "ioc"}, "trigger": {"strategy_type": 0, "price_type": 0, "price": trigger_price_str, "rule": 1_for_sl_or_2_for_tp}, "order_type": "close-long-order" or "close-short-order"}. Sign the request with HMAC-SHA512 per Gate.io v4 API.


        
      
In rust_engine/src/main.rs, in the execution_router_loop(), after the successful fill block (after info!("[execution] SL registered...")), add actual API calls:



if cmd.has_stop_loss() {
    let sl_side = if cmd.side == spsc::side::BUY { OrderSide::Sell } else { OrderSide::Buy };
    match gw.submit_stop_loss(symbol_name, sl_side, FixedPrice(cmd.stop_loss_fp).to_f64(), res.filled_size).await {
        Ok(sl_res) => info!("[execution] SL order submitted: {}", sl_res.order_id),
        Err(e) => error!("[execution] SL submission failed: {} — POSITION UNPROTECTED", e),
    }
}


Do the same for take_profit.
Fix 7 : [HIGH] Dashboard State Not Synced with Live Exchange Data - Balance/Positions Always Zero
Problem

The Rust dashboard shows $0.00 balance, 0 positions, and 0% win rate because the DashboardState is never populated with live exchange data. The execution router only updates a few atomic counters (orders_submitted, orders_rejected) but never calls set_balance(), set_equity(), or set_positions_json() with actual exchange data.
Root Cause

In rust_engine/src/main.rs, the execution_router_loop() function receives dashboard_state: Arc<DashboardState> but only updates it in two places:


        
      
dashboard_state.orders_submitted.store(...) after a fill

        
      
dashboard_state.orders_rejected.store(...) after a rejection

        
      Periodic health check updates active_positions and realized_pnl


It NEVER calls:


        
      
dashboard_state.set_balance(balance) with the actual Gate.io account balance

        
      
dashboard_state.set_equity(equity) with actual equity

        
      
dashboard_state.set_positions_json(json) with actual open positions from the exchange

        
      
dashboard_state.set_unrealized_pnl(pnl) with live unrealized PnL

Files to Fix



        
      
rust_engine/src/main.rs - Add periodic balance/position sync in the execution router's health check block

        
      
rust_engine/src/gateio_gateway.rs - Ensure get_positions() returns properly formatted position data

Prompt for Coding Agent

Fix the dashboard to show live exchange data. In rust_engine/src/main.rs, in the execution_router_loop() function, find the periodic health check block (search for last_health_check.elapsed() > Duration::from_secs(30)). Inside this block, after the existing slot reconciliation code, add:

Fetch and update balance:


if let Some(ref gw) = gateway {
    match gw.get_balance().await {
        Ok(balance) => {
            dashboard_state.set_balance(balance);
            dashboard_state.set_equity(balance); // Will be refined with unrealized PnL
        }
        Err(e) => warn!("[execution] Balance fetch for dashboard failed: {}", e),
    }
}



Fetch and update positions:


if let Some(ref gw) = gateway {
    match gw.get_positions().await {
        Ok(positions) => {
            let pos_json = serde_json::to_string(&positions).unwrap_or("[]".to_string());
            dashboard_state.set_positions_json(pos_json);
            dashboard_state.active_positions.store(positions.len() as u64, Ordering::Relaxed);
            // Calculate total unrealized PnL
            let total_unrealized: f64 = positions.iter()
                .map(|p| p.unrealized_pnl.unwrap_or(0.0))
                .sum();
            dashboard_state.set_unrealized_pnl(total_unrealized);
        }
        Err(e) => warn!("[execution] Position fetch for dashboard failed: {}", e),
    }
}


Also reduce the health check interval from 30s to 10s for more responsive dashboard updates. Change Duration::from_secs(30) to Duration::from_secs(10) for the last_health_check comparison.
Fix 8 : [MEDIUM] Forex Gateway Uses Placeholder Credentials - MT5 Login Shows "your_gateio_tradfi_mt5_login"
Problem

The Forex gateway initializes with placeholder credentials instead of actual values from environment variables.
Log Evidence


rust-engine-1 | INFO trading_engine: [main] Forex gateway enabled: login=your_gateio_tradfi_mt5_login, server=GateIO-TradFi
rust-engine-1 | INFO trading_engine::forex_gateway: [forex] Gateway initialized: login=your_gateio_tradfi_mt5_login, server=GateIO-TradFi


Root Cause

The .env file contains placeholder values like GATEIO_TRADFI_LOGIN=your_gateio_tradfi_mt5_login that are being loaded as actual credentials. The is_valid_key() check is not applied to forex credentials, so any non-empty string passes.
Files to Fix



        
      
rust_engine/src/main.rs - Add validation for forex credentials similar to is_valid_key(). Check that login doesn't contain "your_" or "placeholder" or "example".

        
      
crypto_trading_bot/.env.example - Ensure placeholder values are clearly marked as examples

Prompt for Coding Agent

In rust_engine/src/main.rs, find the forex gateway initialization block (search for "[main] Forex gateway enabled"). Before creating the ForexGateway, add validation:

fn is_valid_forex_credential(v: &str) -> bool {
    !v.is_empty() && !v.contains("your_") && !v.contains("placeholder") && !v.contains("example") && v.len() > 3
}


Then wrap the gateway creation in: if is_valid_forex_credential(login) && is_valid_forex_credential(password). If validation fails, log "[main] Forex gateway disabled — placeholder credentials detected" and set forex_gw = None.
Fix 9 : [MEDIUM] VPIN Microstructure Metric Always Zero - Strategy Signals Degraded
Problem

The VPIN (Volume-Synchronized Probability of Informed Trading) metric is hardcoded to 0.0 in the strategy evaluator, degrading signal quality. VPIN is a critical microstructure indicator for detecting toxic order flow.
Root Cause

In rust_engine/src/main.rs, strategy_evaluator_loop():

let metrics = strategy_engine::MicrostructureMetrics {
    // ...
    vpin: 0.0, // TODO: compute from microstructure module
    last_trade_is_buy: None,
};


The microstructure.rs module exists but is never called from the strategy loop.
Files to Fix



        
      
rust_engine/src/main.rs - Wire the microstructure module into the strategy loop

        
      
rust_engine/src/microstructure.rs - Verify VPIN calculation is implemented

Prompt for Coding Agent

In rust_engine/src/main.rs, in the strategy_evaluator_loop() function, before the MicrostructureMetrics construction, add a call to compute VPIN from the microstructure module. Create a thread-local microstructure::VpinCalculator instance at the top of the function (alongside dust_tracker, exit_evaluator, etc.). On each book snapshot, feed the trade data into the VPIN calculator and use the result:

let mut vpin_calc = microstructure::VpinCalculator::new(50); // 50-bucket VPIN
// ... inside the loop, before metrics construction:
// Feed trade events (update_type == 3) into VPIN
let current_vpin = vpin_calc.current_vpin();
let metrics = strategy_engine::MicrostructureMetrics {
    // ...
    vpin: current_vpin,
    last_trade_is_buy: vpin_calc.last_trade_side(),
};


Also set last_trade_is_buy from the trade data instead of None.


Upgrade 1 : [FEATURE] Add Funding Rate Arbitrage Strategy for Profit Maximization
Feature Description

Gate.io futures have periodic funding rate payments (every 8 hours). When funding rates are extreme (>0.1% per period), there's a reliable arbitrage opportunity: go short when funding is very positive (shorts get paid), go long when funding is very negative (longs get paid). This is a low-risk, consistent profit strategy that should be added.
Implementation Plan

1. Fetch Funding Rates

Gate.io API: GET /futures/usdt/funding_rate?contract=BTC_USDT
Add a periodic funding rate fetcher in the Rust engine that runs every 30 minutes.
2. Strategy Logic



        
      If funding rate > 0.05% (annualized >65%): Open SHORT position to collect funding

        
      If funding rate < -0.05%: Open LONG position to collect funding

        
      Position size: Small (1-2% of portfolio) since this is a carry trade

        
      Hold through the funding settlement, then close if rate normalizes

3. Integration Points



        
      Add FundingRateMonitor to the Rust engine's strategy evaluator

        
      Emit signals through the existing SPSC pipeline

        
      Track funding income separately in PnL attribution

Files to Create/Modify



        
      
rust_engine/src/funding_rate.rs (NEW) - Funding rate fetcher and strategy

        
      
rust_engine/src/main.rs - Wire funding rate monitor into strategy loop

        
      
rust_engine/src/strategy_engine.rs - Add funding rate signal type

Prompt for Coding Agent

Create a new file rust_engine/src/funding_rate.rs that implements a FundingRateMonitor struct. It should:


        
      Periodically fetch funding rates from Gate.io REST API: GET /api/v4/futures/usdt/funding_rate?contract={symbol}&limit=1


        
      Store the latest funding rate per symbol in a HashMap<String, f64>


        
      Provide a method check_funding_opportunity(symbol: &str) -> Option<FundingSignal> that returns a signal when the absolute funding rate exceeds 0.05%

        
      The FundingSignal should include: symbol, direction (SHORT if rate > 0, LONG if rate < 0), rate_pct, annualized_pct, confidence (based on rate magnitude)

In rust_engine/src/main.rs, add mod funding_rate; and create a FundingRateMonitor instance in the strategy evaluator. Check funding opportunities every 1000 book snapshots (approximately every few seconds) and emit OrderCommands when opportunities are found. Add the module declaration alongside the other mod statements at the top of main.rs.

Upgrade 2 : [FEATURE] Add Dynamic Position Sizing Using Kelly Criterion and Volatility Scaling
Feature Description

The current position sizing uses a fixed risk_pct from config. For profit maximization, implement dynamic position sizing that adjusts based on:


        
      
Kelly Criterion: Optimal bet size based on win rate and payoff ratio

        
      
Volatility Scaling: Reduce size in high-volatility regimes, increase in low-vol

        
      
Drawdown Scaling: Reduce size proportionally during drawdowns

        
      
Confidence Weighting: Scale size by signal confidence (from Alpha Oracle)

Implementation Plan

The PositionSizer in rust_engine/src/position_sizer.rs already handles quanto multiplier conversion. Add a new method calculate_dynamic_size() that wraps calculate_contracts() with dynamic risk adjustment.
Kelly Criterion Formula


f* = (p * b - q) / b
where:
  p = win probability
  b = average win / average loss ratio
  q = 1 - p
  
Use half-Kelly (f*/2) for safety


Volatility Scaling


vol_scalar = target_vol / realized_vol
size_multiplier = vol_scalar.clamp(0.25, 2.0)


Drawdown Scaling


if current_drawdown > 0:
    dd_scalar = 1.0 - (current_drawdown / max_allowed_drawdown)
    size_multiplier *= dd_scalar.clamp(0.1, 1.0)


Files to Modify



        
      
rust_engine/src/position_sizer.rs - Add calculate_dynamic_size() method

        
      
rust_engine/src/main.rs - Use dynamic sizing in strategy evaluator

        
      
rust_engine/src/strategy_engine.rs - Pass confidence to sizing

Prompt for Coding Agent

In rust_engine/src/position_sizer.rs, add a new struct DynamicSizingParams with fields: win_rate: f64, avg_win_loss_ratio: f64, realized_volatility: f64, target_volatility: f64, current_drawdown_pct: f64, max_drawdown_pct: f64, signal_confidence: f64. Add a method calculate_dynamic_contracts() to PositionSizer that:


        
      Computes half-Kelly fraction: kelly = ((win_rate * avg_win_loss_ratio - (1.0 - win_rate)) / avg_win_loss_ratio).max(0.0) * 0.5


        
      Computes volatility scalar: vol_scalar = (target_volatility / realized_volatility.max(0.001)).clamp(0.25, 2.0)


        
      Computes drawdown scalar: dd_scalar = (1.0 - current_drawdown_pct / max_drawdown_pct).clamp(0.1, 1.0)


        
      Computes confidence scalar: conf_scalar = signal_confidence.clamp(0.3, 1.0)


        
      Final risk_pct: base_risk_pct * kelly * vol_scalar * dd_scalar * conf_scalar


        
      Calls calculate_contracts() with the adjusted notional

In rust_engine/src/main.rs, in the strategy evaluator, when constructing the OrderCommand, use the dynamic sizing instead of the fixed intent.size. Pass the current regime's volatility estimate and the circuit breaker's drawdown tracking to compute the dynamic params.
Upgrade 3 : [FEATURE] Add TWAP/Iceberg Order Execution for Large Positions
Feature Description

The market impact model already detects when orders are too large and recommends splitting (impact.should_split and impact.recommended_slices), but the actual TWAP (Time-Weighted Average Price) execution is not implemented. Large orders currently execute as single market orders, causing significant slippage.
Current Code (main.rs)


if impact.should_split {
    info!("[strategy] ⚠️ High impact {:.1}bps — recommend TWAP {} slices",
        impact.total_bps, impact.recommended_slices);
}
// ... but then submits as a single order anyway


Implementation Plan



        
      Create a TwapExecutor that splits large orders into N slices

        
      Execute each slice at regular intervals (e.g., every 5-10 seconds)

        
      Track partial fills and adjust remaining slices

        
      Support iceberg orders (show only a fraction of the total size)

Files to Create/Modify



        
      
rust_engine/src/twap_executor.rs (NEW) - TWAP/Iceberg execution engine

        
      
rust_engine/src/main.rs - Route large orders through TWAP executor

Prompt for Coding Agent

Create a new file rust_engine/src/twap_executor.rs implementing a TwapExecutor struct that:


        
      Takes an OrderCommand and splits it into N equal slices based on recommended_slices


        
      Maintains a queue of pending slices with scheduled execution times

        
      Provides tick() method called from the execution loop that submits the next slice when its time arrives

        
      Tracks cumulative fill price for VWAP calculation

        
      Cancels remaining slices if price moves adversely beyond a threshold (e.g., 0.5%)

        
      Reports final execution quality (VWAP vs arrival price, total slippage)

In rust_engine/src/main.rs, add mod twap_executor;. In the execution router, when impact.should_split is true, instead of pushing the full order, create a TwapExecutor instance and add it to a Vec<TwapExecutor> that gets ticked on each idle loop iteration. Each tick checks if it's time to submit the next slice and calls the gateway.

Upgrade 4 : [FEATURE] Add Trailing Stop-Loss with Break-Even Move for Profit Protection
Feature Description

The current exit system uses fixed SL/TP levels. For profit maximization, implement a trailing stop-loss that:


        
      Moves SL to break-even once position is +1% in profit

        
      Trails the price by ATR-based distance as profit increases

        
      Implements partial profit-taking at key levels (25% at 1:1 R:R, 25% at 2:1 R:R, let rest run)

Current Gap

The VolatilityTrailingStop in rust_engine/src/smart_entry.rs calculates trail distances but the ExitEvaluator in rust_engine/src/exit_evaluator.rs doesn't update the exchange-side SL orders. The trailing stop only exists in local memory.
Implementation Plan



        
      When position reaches +1% unrealized PnL, move SL to entry price (break-even)

        
      As price continues in favor, trail SL by ATR * 2.0 distance

        
      At 1:1 R:R, close 25% of position (partial take-profit)

        
      At 2:1 R:R, close another 25%

        
      Let remaining 50% run with trailing stop

Files to Modify



        
      
rust_engine/src/exit_evaluator.rs - Add trailing stop logic with break-even move

        
      
rust_engine/src/main.rs - When exit evaluator updates SL, submit the new SL to exchange via gateway

        
      
rust_engine/src/gateio_gateway.rs - Add modify_stop_loss() method to update existing trigger orders

Prompt for Coding Agent

In rust_engine/src/exit_evaluator.rs, add a TrailingStopState struct per tracked position with fields: initial_sl: f64, current_sl: f64, break_even_triggered: bool, partial_takes: u8. Add method update_trailing_stop(current_price: f64, entry_price: f64, is_long: bool, atr: f64) -> Option<TrailingStopUpdate> that:


        
      Calculates unrealized PnL percentage

        
      If PnL > 1% and !break_even_triggered: set current_sl = entry_price, return TrailingStopUpdate::MoveToBreakEven


        
      If PnL > 2%: trail SL at current_price - (atr * 2.0) for longs (or + atr*2 for shorts), but never move SL backwards

        
      If PnL reaches 1:1 R:R and partial_takes == 0: return TrailingStopUpdate::PartialClose(0.25)


        
      If PnL reaches 2:1 R:R and partial_takes == 1: return TrailingStopUpdate::PartialClose(0.25)


In rust_engine/src/main.rs, in the exit evaluator section of strategy_evaluator_loop(), call update_trailing_stop() on each tick and when it returns an update, push an appropriate OrderCommand to the exec_ring (either a modify-SL command or a partial-close market order).


Strict instructions: Take proper time to read evry fix , issue and upgrades and take proper time to think and design the proper perfect code with full working implementation for all the fixes and upgrades. don't do anything in hurry. Do a proper research for evrything. Before pushing the pr check you must check that there must be no errors remains and all upgrade should be working fine.

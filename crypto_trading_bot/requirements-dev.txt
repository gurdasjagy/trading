# Development and testing dependencies — not included in the production Docker image.
# Install with: pip install -r requirements-dev.txt

-r requirements.txt

# ============ TESTING ============
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
hypothesis>=6.98.0

# ============ RUST EXTENSION BUILD ============
maturin>=1.0,<2.0

# ============ CODE QUALITY ============
black>=24.0.0
ruff>=0.2.0
mypy>=1.8.0

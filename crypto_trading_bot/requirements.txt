# ============ EXCHANGE CONNECTIVITY ============
ccxt>=4.2.0
aiohttp==3.13.3
websockets>=12.0
maturin>=1.0.0
asyncpg==0.30.0
scikit-optimize>=0.10.2
# ============ WEB FRAMEWORK & API ============
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.0
python-multipart>=0.0.6
python-socketio>=5.11.0
httpx>=0.26.0

# ============ CONFIGURATION & ENVIRONMENT ============
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ============ DATABASE & ORM ============
sqlalchemy>=2.0.0
alembic>=1.13.0

# ============ CACHING & TASK QUEUE ============
redis>=5.0.0
celery>=5.3.0

# ============ DATA PROCESSING ============
pandas>=2.1.0
polars>=0.20.0
numpy>=1.26.4
scipy>=1.13.0
pyarrow>=15.0.0
orjson>=3.9.0
matplotlib>=3.8.0

# ============ TECHNICAL ANALYSIS ============
pandas-ta==0.4.71b0

# ============ MACHINE LEARNING ============
scikit-learn>=1.4.0
scikit-optimize>=0.10.0
transformers>=4.36.0
torch>=2.6.0
optuna>=3.4.0
hmmlearn>=0.3.0
stable-baselines3>=2.0.0

# ============ AI / LLM ============
openai>=1.10.0
anthropic>=0.18.0

# ============ SENTIMENT ANALYSIS ============
vaderSentiment>=3.3.2
textblob>=0.18.0
nltk>=3.8.0

# ============ DATA SOURCES ============
telethon>=1.34.0
tweepy>=4.14.0
praw>=7.7.0
feedparser>=6.0.0
beautifulsoup4>=4.12.0
pytrends>=4.9.0
google-api-python-client>=2.100.0

# ============ ZEROMQ ============
pyzmq>=26.0.0

# ============ ASYNC UTILITIES ============
aiofiles>=23.2.0
aiosqlite>=0.19.0

# ============ SCHEDULING ============
apscheduler>=3.10.0

# ============ MONITORING & METRICS ============
prometheus-client>=0.19.0

# ============ SECURITY & CRYPTOGRAPHY ============
cryptography>=42.0.0

# ============ LOGGING & CLI ============
loguru>=0.7.0
rich>=13.7.0
click>=8.1.0

# ============ CROSS-ASSET DATA SOURCES ============
alpha-vantage>=2.3.1
fred>=3.0
yfinance>=0.2.28

# ============ FOREX / MT5 INTEGRATION ============
MetaTrader5>=5.0.45; sys_platform == 'win32'  # Required for Gate.io TradFi MT5 integration
rpyc>=5.4.1  # For Linux/Docker MT5 bridge support

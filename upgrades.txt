╔══════════════════════════════════════════════════════════════════════════════════════╗
║         MASTER CODING AGENT PROMPT — MULTI-EXCHANGE FEATURE IMPLEMENTATION         ║
║                    trade6 Repository — Production-Grade Rust + Python               ║
╚══════════════════════════════════════════════════════════════════════════════════════╝

You are an elite systems engineer implementing a production-grade multi-exchange
feature for an institutional-grade crypto trading bot. This is a ZERO TOLERANCE
environment for pseudo-code, placeholder logic, stub implementations, or TODO
comments. Every line of code you write must be immediately compilable, runnable,
and correct. No exceptions.

═══════════════════════════════════════════════════════════════════════════════════════
SECTION 0: REPOSITORY CONTEXT — READ THIS FIRST
═══════════════════════════════════════════════════════════════════════════════════════

The repository is structured as follows:

  rust_engine/                    ← Rust hot-path engine (the sole executable)
    src/
      main.rs                     ← OS thread spawning, config loading, main()
      config.rs                   ← EngineConfig, ExchangeConfig, RiskConfig, etc.
      gateio_gateway.rs           ← Gate.io WS-based execution gateway
      binance_gateway.rs          ← Binance Futures REST gateway (exists, partial)
      bybit_gateway.rs            ← Bybit v5 REST gateway (exists, partial)
      execution_gateway.rs        ← ExecutionGateway trait + shared types
      dashboard_server.rs         ← axum HTTP+WS dashboard (DashboardState)
      arbitrage_engine.rs         ← Existing arbitrage engine (extend this)
      flat_book.rs                ← FlatOrderBook (lock-free L2 book)
      spsc.rs                     ← SPSC ring buffers (RawBookUpdate, BookSnapshot, etc.)
      circuit_breaker.rs          ← CircuitBreaker
      strategy_engine.rs          ← StrategyEngine
      ... (many more modules)
    Cargo.toml
    Dockerfile

  crypto_trading_bot/
    .env.example                  ← Environment variable documentation
    templates/                    ← Tera (Jinja2) HTML templates
      base.html
      dashboard.html
      trades.html
      performance.html
      risk.html
      settings.html
      logs.html
      ... (forex templates)
    static/                       ← CSS, JS, images

CRITICAL EXISTING ARCHITECTURE FACTS:
1. The Rust engine is the SOLE master process. Python is a cold-path microservice.
2. Gate.io uses a PERSISTENT WebSocket for order execution (gateio_gateway.rs).
   Binance and Bybit currently use REST-only gateways.
3. The ExecutionGateway trait is defined in execution_gateway.rs. All three
   exchanges implement it. The trait includes: submit_order, cancel_order,
   get_position, get_positions, set_leverage, get_balance, get_ticker.
4. The main() function in main.rs spawns OS threads pinned to CPU cores:
   - Core 2: WS Ingestion (Gate.io only currently)
   - Core 3: Orderbook Builder
   - Core 4: Strategy Evaluator
   - Core 5: Execution Router
   - Core 6: Telemetry
5. The DashboardState struct in dashboard_server.rs uses lock-free atomics
   for hot-path metrics and RwLock<String> for JSON blobs (positions, trades).
6. MAX_OPEN_POSITIONS defaults to 3 (from RiskConfig::default_max_open_positions).
7. The PositionSlotManager enforces the hard position limit.
8. The existing ArbitrageConfig in config.rs has enabled_exchanges: Vec<String>.
9. The ThreadTopology struct in config.rs has ws_gateio_core but no fields for
   Binance/Bybit WS cores — you must add them.
10. The apply_env_overrides() function in main.rs reads env vars and injects
    them into EngineConfig — you must extend it for multi-exchange vars.

═══════════════════════════════════════════════════════════════════════════════════════
SECTION 1: THE FEATURE TOGGLE — ABSOLUTE LAW
═══════════════════════════════════════════════════════════════════════════════════════

The ENTIRE multi-exchange feature is gated by a single environment variable:

    USE_MULTI_EXCHANGE=on   → Enable all multi-exchange logic
    USE_MULTI_EXCHANGE=off  → Exact current single-exchange behavior, zero change

ENFORCEMENT RULES (non-negotiable):
- When USE_MULTI_EXCHANGE=off (or missing), the compiled binary must behave
  IDENTICALLY to the current codebase. No performance degradation. No altered
  behavior. No extra allocations. No extra threads.
- When USE_MULTI_EXCHANGE=on, the engine initializes connections, order books,
  and routing logic for Gate.io, Binance, AND Bybit simultaneously.
- The toggle is read ONCE at startup in apply_env_overrides() and stored as
  a bool field `multi_exchange_enabled: bool` on EngineConfig.
- All conditional logic in main.rs, strategy_evaluator_loop, and
  execution_router_loop must branch on this single bool.
- Accepted values for USE_MULTI_EXCHANGE: "on", "true", "1", "yes" (case-insensitive)
  → true. Everything else → false.

═══════════════════════════════════════════════════════════════════════════════════════
SECTION 2: FILES TO CREATE (NEW)
═══════════════════════════════════════════════════════════════════════════════════════

Create the following new files with COMPLETE, FULLY FUNCTIONAL implementations:

──────────────────────────────────────────────────────────────────────────────────────
FILE 1: rust_engine/src/multi_exchange/mod.rs
──────────────────────────────────────────────────────────────────────────────────────

This is the top-level module for all multi-exchange logic. It re-exports:
  pub mod global_book;
  pub mod sor;
  pub mod funding_arb;
  pub mod margin_monitor;
  pub mod ws_ingestion_multi;

──────────────────────────────────────────────────────────────────────────────────────
FILE 2: rust_engine/src/multi_exchange/global_book.rs
──────────────────────────────────────────────────────────────────────────────────────

The Consolidated Global Order Book. This is the core data structure of the
multi-exchange feature.

REQUIRED STRUCTS AND IMPLEMENTATIONS:

```rust
use std::sync::Arc;
use parking_lot::RwLock;
use dashmap::DashMap;
use rust_decimal::Decimal;
use serde::{Deserialize, Serialize};

/// Exchange identifier enum — zero-cost, no heap allocation.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
#[repr(u8)]
pub enum ExchangeId {
    GateIo  = 0,
    Binance = 1,
    Bybit   = 2,
}

impl ExchangeId {
    /// Taker fee in basis points for each exchange.
    pub fn taker_fee_bps(&self) -> i64 { ... }
    /// Maker fee in basis points (negative = rebate).
    pub fn maker_fee_bps(&self) -> i64 { ... }
    /// Display name.
    pub fn name(&self) -> &'static str { ... }
    /// Parse from string (case-insensitive).
    pub fn from_str(s: &str) -> Option<Self> { ... }
}

/// A single price level in the global book, fee-and-latency adjusted.
#[derive(Debug, Clone)]
pub struct GlobalLevel {
    /// Raw price from the exchange (fixed-point, same precision as FlatOrderBook).
    pub raw_price_fp: i64,
    /// Fee-adjusted effective price (raw_price + taker_fee_bps adjustment).
    pub effective_price_fp: i64,
    /// Available quantity at this level (contracts).
    pub qty: i64,
    /// Which exchange this level came from.
    pub exchange: ExchangeId,
    /// Latency penalty in basis points (added to effective_price for asks,
    /// subtracted for bids, based on measured round-trip latency).
    pub latency_penalty_bps: i64,
    /// Timestamp of last update (nanoseconds since epoch).
    pub updated_ns: u64,
}

/// Per-exchange L2 snapshot for a single symbol.
#[derive(Debug, Clone, Default)]
pub struct ExchangeBookSnapshot {
    pub exchange: ExchangeId,
    pub symbol_id: u16,
    pub best_bid_fp: i64,
    pub best_ask_fp: i64,
    pub bid_levels: Vec<(i64, i64)>,  // (price_fp, qty)
    pub ask_levels: Vec<(i64, i64)>,  // (price_fp, qty)
    pub sequence: u64,
    pub timestamp_ns: u64,
}

/// The Consolidated Global Order Book for a single symbol.
/// Merges L2 books from all active exchanges, applying fee and latency
/// adjustments to produce a unified, sorted view of global liquidity.
pub struct GlobalOrderBook {
    pub symbol_id: u16,
    /// Per-exchange snapshots (indexed by ExchangeId as u8).
    exchange_snapshots: [Option<ExchangeBookSnapshot>; 3],
    /// Merged and sorted global bids (best bid first = highest price).
    pub global_bids: Vec<GlobalLevel>,
    /// Merged and sorted global asks (best ask first = lowest price).
    pub global_asks: Vec<GlobalLevel>,
    /// Measured round-trip latency per exchange in microseconds.
    pub latency_us: [u64; 3],
    /// Last full merge timestamp.
    pub last_merge_ns: u64,
}

impl GlobalOrderBook {
    pub fn new(symbol_id: u16) -> Self { ... }

    /// Update the snapshot for a specific exchange and trigger a re-merge.
    /// Called by the WS ingestion threads whenever a new book update arrives.
    pub fn update_exchange_snapshot(&mut self, snapshot: ExchangeBookSnapshot) { ... }

    /// Update measured latency for an exchange (called by WS ingestion on pong).
    pub fn update_latency(&mut self, exchange: ExchangeId, latency_us: u64) { ... }

    /// Merge all exchange snapshots into the global_bids and global_asks vectors.
    /// Applies fee adjustment: effective_ask = raw_ask * (1 + taker_fee_bps/10000)
    ///                         effective_bid = raw_bid * (1 - taker_fee_bps/10000)
    /// Applies latency penalty: latency_penalty_bps = latency_us / 100 (1us = 0.01bps)
    /// Sorts bids descending by effective_price_fp.
    /// Sorts asks ascending by effective_price_fp.
    pub fn merge(&mut self) { ... }

    /// Get the global best bid (highest effective bid across all exchanges).
    pub fn best_bid(&self) -> Option<&GlobalLevel> { ... }

    /// Get the global best ask (lowest effective ask across all exchanges).
    pub fn best_ask(&self) -> Option<&GlobalLevel> { ... }

    /// Calculate global mid price (average of best bid and best ask effective prices).
    pub fn global_mid_fp(&self) -> Option<i64> { ... }

    /// Calculate global spread in basis points.
    pub fn global_spread_bps(&self) -> Option<i64> { ... }

    /// Get total available liquidity at the best N levels across all exchanges.
    pub fn total_bid_depth(&self, levels: usize) -> i64 { ... }
    pub fn total_ask_depth(&self, levels: usize) -> i64 { ... }

    /// Check if a specific exchange's book is stale (last update > 5 seconds ago).
    pub fn is_exchange_stale(&self, exchange: ExchangeId, now_ns: u64) -> bool { ... }
}

/// Thread-safe wrapper for GlobalOrderBook, shared between WS ingestion
/// threads and the strategy evaluator.
pub type SharedGlobalBook = Arc<RwLock<GlobalOrderBook>>;

/// Registry of global books for all symbols.
pub struct GlobalBookRegistry {
    books: DashMap<u16, SharedGlobalBook>,
}

impl GlobalBookRegistry {
    pub fn new() -> Self { ... }
    pub fn get_or_create(&self, symbol_id: u16) -> SharedGlobalBook { ... }
    pub fn get(&self, symbol_id: u16) -> Option<SharedGlobalBook> { ... }
    pub fn all_symbol_ids(&self) -> Vec<u16> { ... }
}
FILE 3: rust_engine/src/multi_exchange/sor.rs
──────────────────────────────────────────────────────────────────────────────────────
Smart Order Router. Determines optimal order splitting across exchanges.
use crate::multi_exchange::global_book::{ExchangeId, GlobalOrderBook};
use crate::execution_gateway::{OrderIntent, OrderSide};

/// A single slice of a split order destined for one exchange.
#[derive(Debug, Clone)]
pub struct OrderSlice {
    pub exchange: ExchangeId,
    pub symbol: String,
    pub side: OrderSide,
    pub size: i64,           // contracts
    pub price_fp: i64,       // limit price (0 = market)
    pub expected_cost_bps: f64,
    pub is_maker: bool,
}

/// Result of the SOR calculation.
#[derive(Debug, Clone)]
pub struct SorResult {
    pub slices: Vec<OrderSlice>,
    pub total_size: i64,
    pub estimated_slippage_bps: f64,
    pub estimated_savings_bps: f64,  // vs. single-exchange execution
    pub routing_reason: String,
}

/// Smart Order Router configuration.
#[derive(Debug, Clone)]
pub struct SorConfig {
    /// Minimum order size (USDT) to trigger splitting across exchanges.
    pub min_split_size_usdt: f64,
    /// Maximum number of exchanges to split across (1-3).
    pub max_venues: usize,
    /// Maximum slippage tolerance in basis points.
    pub max_slippage_bps: f64,
    /// Prefer maker orders when spread allows.
    pub prefer_maker: bool,
}

impl Default for SorConfig {
    fn default() -> Self {
        Self {
            min_split_size_usdt: 5000.0,
            max_venues: 3,
            max_slippage_bps: 30.0,
            prefer_maker: true,
        }
    }
}

pub struct SmartOrderRouter {
    config: SorConfig,
}

impl SmartOrderRouter {
    pub fn new(config: SorConfig) -> Self { ... }

    /// Calculate optimal order routing given the current global book state.
    ///
    /// Algorithm:
    /// 1. If total_size_usdt < min_split_size_usdt → route entirely to best
    ///    single exchange (lowest effective ask for buys, highest effective bid
    ///    for sells).
    /// 2. Otherwise, sweep the global book level by level, allocating size to
    ///    each exchange until the order is fully filled or max_venues is reached.
    /// 3. Apply fee adjustment: prefer exchanges with maker rebates when the
    ///    order can rest on the book (is_maker=true).
    /// 4. Return SorResult with per-exchange slices.
    pub fn route(
        &self,
        book: &GlobalOrderBook,
        side: OrderSide,
        total_size: i64,
        mid_price_fp: i64,
        symbol: &str,
    ) -> SorResult { ... }

    /// Route a single-exchange order (fallback when multi-exchange is off
    /// or when size is below the split threshold).
    pub fn route_single(
        &self,
        exchange: ExchangeId,
        side: OrderSide,
        total_size: i64,
        price_fp: i64,
        symbol: &str,
    ) -> SorResult { ... }
}
──────────────
FILE 4: rust_engine/src/multi_exchange/funding_arb.rs
──────────────────────────────────────────────────────────────────────────────────────
Cross-exchange perpetual funding rate arbitrage engine.
use std::collections::HashMap;
use crate::multi_exchange::global_book::ExchangeId;

/// Funding rate data for a single exchange + symbol.
#[derive(Debug, Clone)]
pub struct FundingRateData {
    pub exchange: ExchangeId,
    pub symbol: String,
    pub rate: f64,           // e.g. 0.0001 = 0.01%
    pub next_funding_ts: u64, // unix timestamp of next funding
    pub updated_ns: u64,
}

/// A detected funding rate arbitrage opportunity.
#[derive(Debug, Clone)]
pub struct FundingArbOpportunity {
    /// Exchange to go SHORT on (high positive funding = shorts receive payment).
    pub short_exchange: ExchangeId,
    /// Exchange to go LONG on (low or negative funding = longs receive payment).
    pub long_exchange: ExchangeId,
    pub symbol: String,
    /// Net funding rate spread (short_rate - long_rate).
    pub net_rate: f64,
    /// Annualized APR of the spread.
    pub annualized_apr: f64,
    /// Estimated net profit after fees (basis points per funding period).
    pub net_profit_bps: f64,
    /// Whether this opportunity is currently actionable.
    pub is_actionable: bool,
}

/// Cross-exchange funding rate arbitrage monitor.
pub struct CrossExchangeFundingArb {
    /// Latest funding rates per exchange per symbol.
    rates: HashMap<(ExchangeId, String), FundingRateData>,
    /// Minimum net rate spread to consider actionable (default: 0.005% = 0.5bps).
    min_net_rate: f64,
    /// Minimum annualized APR to consider actionable (default: 10%).
    min_annualized_apr: f64,
}

impl CrossExchangeFundingArb {
    pub fn new(min_net_rate: f64, min_annualized_apr: f64) -> Self { ... }

    /// Update funding rate data for a specific exchange and symbol.
    pub fn update_rate(&mut self, data: FundingRateData) { ... }

    /// Scan all known symbols for cross-exchange funding arbitrage opportunities.
    /// Returns a list of actionable opportunities sorted by net_profit_bps descending.
    pub fn scan_opportunities(&self) -> Vec<FundingArbOpportunity> { ... }

    /// Check if a specific symbol has an actionable opportunity.
    pub fn check_symbol(&self, symbol: &str) -> Option<FundingArbOpportunity> { ... }

    /// Fetch funding rates from Binance Futures REST API.
    /// Endpoint: GET /fapi/v1/premiumIndex?symbol={symbol}
    /// Returns the fundingRate field as f64.
    pub async fn fetch_binance_funding_rate(
        client: &reqwest::Client,
        symbol: &str,
        testnet: bool,
    ) -> Option<FundingRateData> { ... }

    /// Fetch funding rates from Bybit v5 REST API.
    /// Endpoint: GET /v5/market/tickers?category=linear&symbol={symbol}
    /// Returns the fundingRate field from result.list[0].
    pub async fn fetch_bybit_funding_rate(
        client: &reqwest::Client,
        symbol: &str,
        testnet: bool,
    ) -> Option<FundingRateData> { ... }

    /// Fetch funding rates from Gate.io REST API.
    /// Endpoint: GET /futures/usdt/tickers?contract={symbol}
    /// Returns the funding_rate field.
    pub async fn fetch_gateio_funding_rate(
        client: &reqwest::Client,
        symbol: &str,
        testnet: bool,
    ) -> Option<FundingRateData> { ... }
}
FILE 5: rust_engine/src/multi_exchange/margin_monitor.rs
──────────────────────────────────────────────────────────────────────────────────────
Cross-venue inventory and margin management.
use std::collections::HashMap;
use crate::multi_exchange::global_book::ExchangeId;
use crate::execution_gateway::ExecutionGateway;
use std::sync::Arc;

/// Margin health status for a single exchange.
#[derive(Debug, Clone)]
pub struct ExchangeMarginHealth {
    pub exchange: ExchangeId,
    pub available_balance: f64,
    pub total_equity: f64,
    pub unrealized_pnl: f64,
    pub margin_ratio: f64,   // available / total_equity
    pub is_healthy: bool,    // margin_ratio > 0.3 (30% threshold)
    pub updated_ns: u64,
}

/// Cross-venue margin imbalance alert.
#[derive(Debug, Clone)]
pub struct MarginImbalanceAlert {
    pub critical_exchange: ExchangeId,
    pub margin_ratio: f64,
    pub recommended_transfer_usdt: f64,
    pub source_exchange: ExchangeId,  // exchange with excess margin
}

/// Cross-venue margin monitor. Runs on the cold path (every 30 seconds).
pub struct CrossVenueMarginMonitor {
    /// Latest margin health per exchange.
    health: HashMap<ExchangeId, ExchangeMarginHealth>,
    /// Minimum acceptable margin ratio before alert (default: 0.30 = 30%).
    min_margin_ratio: f64,
    /// Critical margin ratio threshold (default: 0.15 = 15%).
    critical_margin_ratio: f64,
}

impl CrossVenueMarginMonitor {
    pub fn new(min_margin_ratio: f64, critical_margin_ratio: f64) -> Self { ... }

    /// Update margin health for a specific exchange.
    pub fn update_health(&mut self, health: ExchangeMarginHealth) { ... }

    /// Check for margin imbalances across all exchanges.
    /// Returns alerts for exchanges below the minimum margin ratio.
    pub fn check_imbalances(&self) -> Vec<MarginImbalanceAlert> { ... }

    /// Calculate global delta neutrality.
    /// Returns (total_long_usdt, total_short_usdt, net_delta_usdt).
    pub fn calculate_global_delta(
        &self,
        positions: &HashMap<ExchangeId, Vec<(String, f64, f64)>>, // (symbol, size, price)
    ) -> (f64, f64, f64) { ... }

    /// Fetch and update margin health from all active gateways.
    /// Called every 30 seconds from the execution router loop.
    pub async fn refresh_all(
        &mut self,
        gateways: &HashMap<ExchangeId, Arc<dyn ExecutionGateway + Send + Sync>>,
    ) { ... }
}
FILE 6: rust_engine/src/multi_exchange/ws_ingestion_multi.rs
──────────────────────────────────────────────────────────────────────────────────────
Multi-exchange WebSocket ingestion. Runs parallel WS connections for Binance
and Bybit alongside the existing Gate.io connection.
use std::sync::Arc;
use std::time::Duration;
use futures_util::{SinkExt, StreamExt};
use tokio_tungstenite::{connect_async, tungstenite::Message};
use tracing::{info, warn, error, debug};

use crate::config::ExchangeConfig;
use crate::multi_exchange::global_book::{
    ExchangeBookSnapshot, ExchangeId, GlobalBookRegistry,
};
use crate::fixed_point::FixedPrice;

// ── Binance WebSocket URLs ──────────────────────────────────────────────────
const BINANCE_WS_LIVE: &str = "wss://fstream.binance.com/stream";
const BINANCE_WS_TESTNET: &str = "wss://stream.binancefuture.com/stream";

// ── Bybit WebSocket URLs ────────────────────────────────────────────────────
const BYBIT_WS_LIVE: &str = "wss://stream.bybit.com/v5/public/linear";
const BYBIT_WS_TESTNET: &str = "wss://stream-testnet.bybit.com/v5/public/linear";

/// Spawn a Binance Futures WebSocket ingestion task.
///
/// Subscribes to `{symbol}@depth20@100ms` for each configured symbol.
/// Parses the Binance depth update format:
///   { "b": [["price", "qty"], ...], "a": [["price", "qty"], ...] }
/// Updates the GlobalBookRegistry with ExchangeBookSnapshot on each message.
///
/// Implements automatic reconnection with exponential backoff (500ms → 30s).
/// On sequence gap detection (lastUpdateId mismatch), resubscribes immediately.
pub async fn run_binance_ws_ingestion(
    config: ExchangeConfig,
    registry: Arc<GlobalBookRegistry>,
    symbol_ids: Arc<crate::config::SymbolRegistry>,
) {
    let ws_url = if config.testnet { BINANCE_WS_TESTNET } else { BINANCE_WS_LIVE };
    let mut backoff_ms = 500u64;

    loop {
        info!("[ws-binance] Connecting to {}", ws_url);

        // Build combined stream URL: /stream?streams=btcusdt@depth20@100ms/ethusdt@depth20@100ms
        let streams: Vec<String> = config.symbols.iter()
            .map(|s| {
                let normalized = s.replace('_', "").to_lowercase();
                format!("{}@depth20@100ms", normalized)
            })
            .collect();
        let stream_param = streams.join("/");
        let full_url = format!("{}?streams={}", ws_url, stream_param);

        match connect_async(&full_url).await {
            Ok((ws_stream, _)) => {
                info!("[ws-binance] Connected, subscribed to {} symbols", config.symbols.len());
                backoff_ms = 500;

                let (mut write, mut read) = ws_stream.split();
                let mut ping_interval = tokio::time::interval(Duration::from_secs(20));
                ping_interval.tick().await;

                loop {
                    tokio::select! {
                        msg = read.next() => {
                            match msg {
                                Some(Ok(Message::Text(text))) => {
                                    // Parse Binance combined stream format:
                                    // { "stream": "btcusdt@depth20@100ms", "data": { ... } }
                                    if let Ok(parsed) = serde_json::from_str::<serde_json::Value>(&text) {
                                        let stream_name = parsed.get("stream")
                                            .and_then(|v| v.as_str())
                                            .unwrap_or("");
                                        let data = parsed.get("data").unwrap_or(&parsed);

                                        // Extract symbol from stream name: "btcusdt@depth20@100ms" → "BTCUSDT"
                                        let symbol_upper = stream_name
                                            .split('@')
                                            .next()
                                            .unwrap_or("")
                                            .to_uppercase();

                                        // Map Binance symbol to our internal symbol_id
                                        // "BTCUSDT" → "BTC_USDT" → symbol_id lookup
                                        let internal_symbol = format!("{}_USDT",
                                            symbol_upper.trim_end_matches("USDT"));
                                        let sym_id = symbol_ids.get_id(&internal_symbol);
                                        if sym_id == 0 { continue; }

                                        let now_ns = std::time::SystemTime::now()
                                            .duration_since(std::time::UNIX_EPOCH)
                                            .unwrap_or_default()
                                            .as_nanos() as u64;

                                        // Parse bids and asks
                                        let mut bid_levels = Vec::new();
                                        let mut ask_levels = Vec::new();

                                        if let Some(bids) = data.get("b").and_then(|v| v.as_array()) {
                                            for level in bids {
                                                if let Some(arr) = level.as_array() {
                                                    if arr.len() >= 2 {
                                                        let price: f64 = arr[0].as_str()
                                                            .and_then(|s| s.parse().ok())
                                                            .unwrap_or(0.0);
                                                        let qty: f64 = arr[1].as_str()
                                                            .and_then(|s| s.parse().ok())
                                                            .unwrap_or(0.0);
                                                        if price > 0.0 {
                                                            bid_levels.push((
                                                                FixedPrice::from_f64(price).raw(),
                                                                (qty * 1e8) as i64,
                                                            ));
                                                        }
                                                    }
                                                }
                                            }
                                        }

                                        if let Some(asks) = data.get("a").and_then(|v| v.as_array()) {
                                            for level in asks {
                                                if let Some(arr) = level.as_array() {
                                                    if arr.len() >= 2 {
                                                        let price: f64 = arr[0].as_str()
                                                            .and_then(|s| s.parse().ok())
                                                            .unwrap_or(0.0);
                                                        let qty: f64 = arr[1].as_str()
                                                            .and_then(|s| s.parse().ok())
                                                            .unwrap_or(0.0);
                                                        if price > 0.0 {
                                                            ask_levels.push((
                                                                FixedPrice::from_f64(price).raw(),
                                                                (qty * 1e8) as i64,
                                                            ));
                                                        }
                                                    }
                                                }
                                            }
                                        }

                                        let best_bid = bid_levels.iter()
                                            .map(|(p, _)| *p)
                                            .max()
                                            .unwrap_or(0);
                                        let best_ask = ask_levels.iter()
                                            .map(|(p, _)| *p)
                                            .min()
                                            .unwrap_or(0);

                                        let snapshot = ExchangeBookSnapshot {
                                            exchange: ExchangeId::Binance,
                                            symbol_id: sym_id,
                                            best_bid_fp: best_bid,
                                            best_ask_fp: best_ask,
                                            bid_levels,
                                            ask_levels,
                                            sequence: data.get("u")
                                                .and_then(|v| v.as_u64())
                                                .unwrap_or(0),
                                            timestamp_ns: now_ns,
                                        };

                                        let book = registry.get_or_create(sym_id);
                                        book.write().update_exchange_snapshot(snapshot);
                                    }
                                }
                                Some(Ok(Message::Ping(data))) => {
                                    let _ = write.send(Message::Pong(data)).await;
                                }
                                Some(Ok(Message::Close(_))) => {
                                    warn!("[ws-binance] Server closed connection");
                                    break;
                                }
                                Some(Err(e)) => {
                                    error!("[ws-binance] Read error: {}", e);
                                    break;
                                }
                                None => break,
                                _ => {}
                            }
                        }
                        _ = ping_interval.tick() => {
                            // Binance requires periodic pong responses; send a ping
                            if let Err(e) = write.send(Message::Ping(vec![])).await {
                                warn!("[ws-binance] Ping failed: {}", e);
                                break;
                            }
                        }
                    }
                }
            }
            Err(e) => {
                error!("[ws-binance] Connection failed: {}", e);
            }
        }

        warn!("[ws-binance] Reconnecting in {}ms...", backoff_ms);
        tokio::time::sleep(Duration::from_millis(backoff_ms)).await;
        backoff_ms = (backoff_ms * 2).min(30_000);
    }
}

/// Spawn a Bybit v5 WebSocket ingestion task.
///
/// Subscribes to `orderbook.20.{SYMBOL}` for each configured symbol.
/// Parses the Bybit v5 orderbook format:
///   { "topic": "orderbook.20.BTCUSDT", "type": "snapshot"|"delta",
///     "data": { "b": [["price", "qty"], ...], "a": [...] } }
/// Updates the GlobalBookRegistry with ExchangeBookSnapshot on each message.
///
/// Bybit v5 uses a subscription message after connection:
///   { "op": "subscribe", "args": ["orderbook.20.BTCUSDT", ...] }
///
/// Implements automatic reconnection with exponential backoff.
pub async fn run_bybit_ws_ingestion(
    config: ExchangeConfig,
    registry: Arc<GlobalBookRegistry>,
    symbol_ids: Arc<crate::config::SymbolRegistry>,
) {
    let ws_url = if config.testnet { BYBIT_WS_TESTNET } else { BYBIT_WS_LIVE };
    let mut backoff_ms = 500u64;

    loop {
        info!("[ws-bybit] Connecting to {}", ws_url);

        match connect_async(ws_url).await {
            Ok((ws_stream, _)) => {
                info!("[ws-bybit] Connected");
                backoff_ms = 500;

                let (mut write, mut read) = ws_stream.split();

                // Subscribe to orderbook for all symbols
                let args: Vec<String> = config.symbols.iter()
                    .map(|s| {
                        // "BTC_USDT" → "BTCUSDT"
                        let bybit_sym = s.replace('_', "").to_uppercase();
                        format!("orderbook.20.{}", bybit_sym)
                    })
                    .collect();

                let sub_msg = serde_json::json!({
                    "op": "subscribe",
                    "args": args
                });

                if let Err(e) = write.send(Message::Text(sub_msg.to_string())).await {
                    error!("[ws-bybit] Subscribe failed: {}", e);
                    continue;
                }

                let mut ping_interval = tokio::time::interval(Duration::from_secs(20));
                ping_interval.tick().await;

                loop {
                    tokio::select! {
                        msg = read.next() => {
                            match msg {
                                Some(Ok(Message::Text(text))) => {
                                    if let Ok(parsed) = serde_json::from_str::<serde_json::Value>(&text) {
                                        // Handle subscription confirmation
                                        if parsed.get("op").and_then(|v| v.as_str()) == Some("subscribe") {
                                            let success = parsed.get("success")
                                                .and_then(|v| v.as_bool())
                                                .unwrap_or(false);
                                            if success {
                                                info!("[ws-bybit] Subscription confirmed");
                                            } else {
                                                error!("[ws-bybit] Subscription failed: {}", text);
                                            }
                                            continue;
                                        }

                                        // Handle orderbook updates
                                        let topic = parsed.get("topic")
                                            .and_then(|v| v.as_str())
                                            .unwrap_or("");

                                        if !topic.starts_with("orderbook.") { continue; }

                                        // Extract symbol: "orderbook.20.BTCUSDT" → "BTCUSDT"
                                        let bybit_symbol = topic.split('.').nth(2).unwrap_or("");
                                        // Map "BTCUSDT" → "BTC_USDT"
                                        let internal_symbol = format!("{}_USDT",
                                            bybit_symbol.trim_end_matches("USDT"));
                                        let sym_id = symbol_ids.get_id(&internal_symbol);
                                        if sym_id == 0 { continue; }

                                        let data = match parsed.get("data") {
                                            Some(d) => d,
                                            None => continue,
                                        };

                                        let now_ns = std::time::SystemTime::now()
                                            .duration_since(std::time::UNIX_EPOCH)
                                            .unwrap_or_default()
                                            .as_nanos() as u64;

                                        let mut bid_levels = Vec::new();
                                        let mut ask_levels = Vec::new();

                                        // Bybit format: "b": [["price", "qty"], ...]
                                        if let Some(bids) = data.get("b").and_then(|v| v.as_array()) {
                                            for level in bids {
                                                if let Some(arr) = level.as_array() {
                                                    if arr.len() >= 2 {
                                                        let price: f64 = arr[0].as_str()
                                                            .and_then(|s| s.parse().ok())
                                                            .unwrap_or(0.0);
                                                        let qty: f64 = arr[1].as_str()
                                                            .and_then(|s| s.parse().ok())
                                                            .unwrap_or(0.0);
                                                        if price > 0.0 {
                                                            bid_levels.push((
                                                                FixedPrice::from_f64(price).raw(),
                                                                (qty * 1e8) as i64,
                                                            ));
                                                        }
                                                    }
                                                }
                                            }
                                        }

                                        if let Some(asks) = data.get("a").and_then(|v| v.as_array()) {
                                            for level in asks {
                                                if let Some(arr) = level.as_array() {
                                                    if arr.len() >= 2 {
                                                        let price: f64 = arr[0].as_str()
                                                            .and_then(|s| s.parse().ok())
                                                            .unwrap_or(0.0);
                                                        let qty: f64 = arr[1].as_str()
                                                            .and_then(|s| s.parse().ok())
                                                            .unwrap_or(0.0);
                                                        if price > 0.0 {
                                                            ask_levels.push((
                                                                FixedPrice::from_f64(price).raw(),
                                                                (qty * 1e8) as i64,
                                                            ));
                                                        }
                                                    }
                                                }
                                            }
                                        }

                                        let best_bid = bid_levels.iter()
                                            .map(|(p, _)| *p)
                                            .max()
                                            .unwrap_or(0);
                                        let best_ask = ask_levels.iter()
                                            .map(|(p, _)| *p)
                                            .min()
                                            .unwrap_or(0);

                                        let seq = parsed.get("ts")
                                            .and_then(|v| v.as_u64())
                                            .unwrap_or(0);

                                        let snapshot = ExchangeBookSnapshot {
                                            exchange: ExchangeId::Bybit,
                                            symbol_id: sym_id,
                                            best_bid_fp: best_bid,
                                            best_ask_fp: best_ask,
                                            bid_levels,
                                            ask_levels,
                                            sequence: seq,
                                            timestamp_ns: now_ns,
                                        };

                                        let book = registry.get_or_create(sym_id);
                                        book.write().update_exchange_snapshot(snapshot);
                                    }
                                }
                                Some(Ok(Message::Ping(data))) => {
                                    let _ = write.send(Message::Pong(data)).await;
                                }
                                Some(Ok(Message::Close(_))) => {
                                    warn!("[ws-bybit] Server closed connection");
                                    break;
                                }
                                Some(Err(e)) => {
                                    error!("[ws-bybit] Read error: {}", e);
                                    break;
                                }
                                None => break,
                                _ => {}
                            }
                        }
                        _ = ping_interval.tick() => {
                            // Bybit heartbeat: send {"op": "ping"}
                            let ping = serde_json::json!({"op": "ping"});
                            if let Err(e) = write.send(Message::Text(ping.to_string())).await {
                                warn!("[ws-bybit] Ping failed: {}", e);
                                break;
                            }
                        }
                    }
                }
            }
            Err(e) => {
                error!("[ws-bybit] Connection failed: {}", e);
            }
        }

        warn!("[ws-bybit] Reconnecting in {}ms...", backoff_ms);
        tokio::time::sleep(Duration::from_millis(backoff_ms)).await;
        backoff_ms = (backoff_ms * 2).min(30_000);
    }
}
FILE 7: crypto_trading_bot/templates/multi_exchange.html
──────────────────────────────────────────────────────────────────────────────────────
A new dedicated dashboard page for multi-exchange portfolio view. This template
extends base.html and must be a complete, fully functional Tera template.
REQUIRED SECTIONS:

Multi-Exchange Portfolio Summary Card

Total equity aggregated across all exchanges (Gate.io + Binance + Bybit)
Per-exchange balance breakdown with colored exchange badges:
Gate.io = blue badge, Binance = yellow badge, Bybit = orange badge
Total unrealized PnL across all exchanges
Global delta (net long/short exposure in USDT)
Margin health indicator per exchange (green/yellow/red)


Active Trades Table (replaces single-exchange trades table when multi-exchange is on)

Columns: Exchange (badge), Symbol, Direction, Size, Entry Price,
Current Price, Unrealized PnL, PnL%, Leverage, SL, TP, Duration, Actions
Exchange badge: colored pill showing "Gate.io", "Binance", or "Bybit"
Each row has a "Close" button that calls POST /api/positions/{exchange}/{symbol}/close
Rows are sorted by unrealized PnL descending (most profitable first)
Maximum 5 rows shown (enforced by MAX_OPEN_POSITIONS=5 when multi-exchange is on)


Cross-Exchange Funding Rate Arbitrage Panel

Table showing current funding rates per exchange per symbol
Columns: Symbol, Gate.io Rate, Binance Rate, Bybit Rate, Best Spread, Action
"Best Spread" column highlights the highest cross-exchange spread in green
"Action" column shows "Open Arb" button when spread > 0.01%


Global Order Book Visualization

Side-by-side comparison of best bid/ask from each exchange
Shows fee-adjusted effective prices
Highlights the globally best bid and ask with a star icon


Smart Order Router Preview

Input: Symbol, Side (Buy/Sell), Size (USDT)
Output: Calculated order split across exchanges with estimated slippage
"Preview Route" button calls GET /api/multi-exchange/sor-preview


Cross-Exchange Margin Health Panel

Per-exchange margin ratio gauge (0-100%)
Alert banner when any exchange margin ratio < 30%
"Rebalance" button (calls POST /api/multi-exchange/rebalance)


Real-time WebSocket updates

JavaScript that connects to /ws/live and updates all panels on each message
Specifically handles the new "multi_exchange" message type in the WS payload



The template must use the existing CSS classes from base.html and be consistent
with the existing dashboard visual style (dark theme, Bootstrap-compatible).
──────────────────────────────────────────────────────────────────────────────────────
FILE 8: crypto_trading_bot/templates/dashboard.html (MODIFY EXISTING)
──────────────────────────────────────────────────────────────────────────────────────
Modify the existing dashboard.html to add multi-exchange awareness:

Add a conditional banner at the top when USE_MULTI_EXCHANGE=on:
{% if use_multi_exchange %}

  Multi-Exchange Mode Active — Gate.io + Binance + Bybit
  View Global Portfolio

{% endif %}

In the Active Trades section, add exchange badge to each trade row:
{% if trade.exchange %}
{{ trade.exchange }}
{% endif %}

Update the "Max Open Positions" display to show 5 when multi-exchange is on,
3 when off:
Max: {{ max_open_positions }}

Add a navigation link to /multi-exchange in the sidebar when multi-exchange is on.


═══════════════════════════════════════════════════════════════════════════════════════
SECTION 3: FILES TO MODIFY (EXISTING)
═══════════════════════════════════════════════════════════════════════════════════════
──────────────────────────────────────────────────────────────────────────────────────
MODIFY: rust_engine/src/config.rs
──────────────────────────────────────────────────────────────────────────────────────

Add multi_exchange_enabled: bool field to EngineConfig with default false.
Add MultiExchangeConfig struct
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MultiExchangeConfig {
    /// Master toggle — mirrors USE_MULTI_EXCHANGE env var.
    #[serde(default)]
    pub enabled: bool,
    /// Binance Futures API credentials.
    pub binance_api_key: Option<String>,
    pub binance_secret_key: Option<String>,
    #[serde(default)]
    pub binance_testnet: bool,
    /// Bybit v5 API credentials.
    pub bybit_api_key: Option<String>,
    pub bybit_secret_key: Option<String>,
    #[serde(default)]
    pub bybit_testnet: bool,
    /// SOR configuration.
    #[serde(default)]
    pub sor: SorConfigToml,
    /// Funding arb configuration.
    #[serde(default)]
    pub funding_arb: FundingArbConfigToml,
    /// Margin monitor configuration.
    #[serde(default)]
    pub margin_monitor: MarginMonitorConfigToml,
    /// Maximum open positions when multi-exchange is enabled (default: 5).
    #[serde(default = "default_multi_max_positions")]
    pub max_open_positions: u32,
}

fn default_multi_max_positions() -> u32 { 5 }

impl Default for MultiExchangeConfig {
    fn default() -> Self { ... }
}
Add SorConfigToml, FundingArbConfigToml, MarginMonitorConfigToml structs
with serde defaults matching the defaults in the new module files.

Add multi_exchange: MultiExchangeConfig field to EngineConfig.

Add ws_binance_core: usize and ws_bybit_core: usize fields to ThreadTopology
with defaults of 12 and 13 respectively. Update auto_detect() to assign these
cores only when cpu_count >= 14, otherwise share with ws_gateio_core.


──────────────────────────────────────────────────────────────────────────────────────
MODIFY: rust_engine/src/main.rs
──────────────────────────────────────────────────────────────────────────────────────

Add mod multi_exchange; at the top of the module declarations.

In apply_env_overrides(), add multi-exchange env var parsing:
// ── Multi-Exchange Toggle ──────────────────────────────────────────────────
let use_multi = std::env::var("USE_MULTI_EXCHANGE")
    .map(|v| matches!(v.to_lowercase().as_str(), "on" | "true" | "1" | "yes"))
    .unwrap_or(false);
cfg.multi_exchange_enabled = use_multi;
cfg.multi_exchange.enabled = use_multi;

if use_multi {
    // Binance credentials
    cfg.multi_exchange.binance_api_key = std::env::var("BINANCE_API_KEY")
        .ok().map(|s| s.trim().to_string());
    cfg.multi_exchange.binance_secret_key = std::env::var("BINANCE_SECRET_KEY")
        .ok().map(|s| s.trim().to_string());
    cfg.multi_exchange.binance_testnet = std::env::var("BINANCE_TESTNET")
        .map(|v| v == "true" || v == "1")
        .unwrap_or(false);

    // Bybit credentials
    cfg.multi_exchange.bybit_api_key = std::env::var("BYBIT_API_KEY")
        .ok().map(|s| s.trim().to_string());
    cfg.multi_exchange.bybit_secret_key = std::env::var("BYBIT_SECRET_KEY")
        .ok().map(|s| s.trim().to_string());
    cfg.multi_exchange.bybit_testnet = std::env::var("BYBIT_TESTNET")
        .map(|v| v == "true" || v == "1")
        .unwrap_or(false);

    // Override max open positions to 5 when multi-exchange is on
    cfg.risk.max_open_positions = cfg.multi_exchange.max_open_positions as usize;
    eprintln!("[config] USE_MULTI_EXCHANGE=on → max_open_positions={}", cfg.risk.max_open_positions);
} else {
    eprintln!("[config] USE_MULTI_EXCHANGE=off → single-exchange mode (Gate.io only)");
}
In build_gateway(), extend to handle Binance and Bybit:
fn build_gateway(cfg: &ExchangeConfig) -> Option<Arc<dyn ExecutionGateway + Send + Sync>> {
    let api_key = cfg.api_key.as_deref().unwrap_or("");
    let secret_key = cfg.secret_key.as_deref().unwrap_or("");
    if !is_valid_key(api_key) || !is_valid_key(secret_key) {
        info!("{}: no valid API credentials — signal-only mode", cfg.name);
        return None;
    }
    match cfg.name.as_str() {
        "gateio" => {
            info!("{}: live execution gateway initialised", cfg.name);
            Some(Arc::new(GateIoGateway::new(
                api_key.to_string(), secret_key.to_string(), cfg.testnet,
            )))
        }
        "binance" => {
            info!("{}: Binance Futures gateway initialised (testnet={})", cfg.name, cfg.testnet);
            Some(Arc::new(BinanceGateway::new(
                api_key.to_string(), secret_key.to_string(), cfg.testnet,
            )))
        }
        "bybit" => {
            info!("{}: Bybit v5 gateway initialised (testnet={})", cfg.name, cfg.testnet);
            Some(Arc::new(BybitGateway::new(
                api_key.to_string(), secret_key.to_string(), cfg.testnet,
            )))
        }
        other => {
            warn!("{}: no execution gateway implementation — signal-only mode", other);
            None
        }
    }
}
In main(), after the existing gateway initialization block, add the
multi-exchange initialization block:
// ── Multi-Exchange Initialization (gated by USE_MULTI_EXCHANGE) ──────────
let (
    global_book_registry,
    binance_gateway,
    bybit_gateway,
    multi_exchange_gateways,
) = if config.multi_exchange_enabled {
    info!("🌐 Multi-Exchange mode ENABLED — initializing Binance + Bybit");

    // Build Binance gateway
    let binance_gw: Option<Arc<dyn ExecutionGateway + Send + Sync>> = {
        let ak = config.multi_exchange.binance_api_key.as_deref().unwrap_or("");
        let sk = config.multi_exchange.binance_secret_key.as_deref().unwrap_or("");
        if is_valid_key(ak) && is_valid_key(sk) {
            info!("Binance: live execution gateway initialised (testnet={})",
                  config.multi_exchange.binance_testnet);
            Some(Arc::new(BinanceGateway::new(
                ak.to_string(), sk.to_string(),
                config.multi_exchange.binance_testnet,
            )))
        } else {
            info!("Binance: no valid credentials — signal-only mode");
            None
        }
    };

    // Build Bybit gateway
    let bybit_gw: Option<Arc<dyn ExecutionGateway + Send + Sync>> = {
        let ak = config.multi_exchange.bybit_api_key.as_deref().unwrap_or("");
        let sk = config.multi_exchange.bybit_secret_key.as_deref().unwrap_or("");
        if is_valid_key(ak) && is_valid_key(sk) {
            info!("Bybit: live execution gateway initialised (testnet={})",
                  config.multi_exchange.bybit_testnet);
            Some(Arc::new(BybitGateway::new(
                ak.to_string(), sk.to_string(),
                config.multi_exchange.bybit_testnet,
            )))
        } else {
            info!("Bybit: no valid credentials — signal-only mode");
            None
        }
    };

    // Build global book registry
    let registry = Arc::new(multi_exchange::global_book::GlobalBookRegistry::new());

    // Build multi-exchange gateway map
    let mut gw_map: HashMap<multi_exchange::global_book::ExchangeId,
                            Arc<dyn ExecutionGateway + Send + Sync>> = HashMap::new();
    if let Some(ref gw) = gateway {
        gw_map.insert(multi_exchange::global_book::ExchangeId::GateIo, gw.clone());
    }
    if let Some(ref gw) = binance_gw {
        gw_map.insert(multi_exchange::global_book::ExchangeId::Binance, gw.clone());
    }
    if let Some(ref gw) = bybit_gw {
        gw_map.insert(multi_exchange::global_book::ExchangeId::Bybit, gw.clone());
    }

    (Some(registry), binance_gw, bybit_gw, gw_map)
} else {
    info!("🔒 Multi-Exchange mode DISABLED — Gate.io only");
    (None, None, None, HashMap::new())
};
In the thread spawning section, add Binance and Bybit WS ingestion threads
ONLY when multi_exchange_enabled is true:
// ── Multi-Exchange WS Ingestion Threads ──────────────────────────────────
if config.multi_exchange_enabled {
    if let Some(ref registry) = global_book_registry {
        // Binance WS ingestion thread
        {
            let reg = registry.clone();
            let sym_reg = registry_arc.clone();
            let binance_cfg = ExchangeConfig {
                name: "binance".to_string(),
                symbols: config.symbols.clone(),
                ws_url: if config.multi_exchange.binance_testnet {
                    "wss://stream.binancefuture.com/stream".to_string()
                } else {
                    "wss://fstream.binance.com/stream".to_string()
                },
                api_key: config.multi_exchange.binance_api_key.clone(),
                secret_key: config.multi_exchange.binance_secret_key.clone(),
                passphrase: None,
                testnet: config.multi_exchange.binance_testnet,
                rest_url: None,
                max_leverage: 125,
                enabled: true,
            };
            let core_id = topology.ws_binance_core;
            let handle = thread::Builder::new()
                .name("ws-binance".into())
                .spawn(move || {
                    let _ = core_affinity::set_for_current(
                        core_affinity::CoreId { id: core_id }
                    );
                    info!("[ws-binance] Pinned to core {}", core_id);
                    let rt = tokio::runtime::Builder::new_current_thread()
                        .enable_all()
                        .build()
                        .expect("Failed to build tokio runtime for ws-binance");
                    rt.block_on(
                        multi_exchange::ws_ingestion_multi::run_binance_ws_ingestion(
                            binance_cfg, reg, sym_reg,
                        )
                    );
                })
                .expect("Failed to spawn Binance WS thread");
            thread_handles.push(handle);
        }

        // Bybit WS ingestion thread
        {
            let reg = registry.clone();
            let sym_reg = registry_arc.clone();
            let bybit_cfg = ExchangeConfig {
                name: "bybit".to_string(),
                symbols: config.symbols.clone(),
                ws_url: if config.multi_exchange.bybit_testnet {
                    "wss://stream-testnet.bybit.com/v5/public/linear".to_string()
                } else {
                    "wss://stream.bybit.com/v5/public/linear".to_string()
                },
                api_key: config.multi_exchange.bybit_api_key.clone(),
                secret_key: config.multi_exchange.bybit_secret_key.clone(),
                passphrase: None,
                testnet: config.multi_exchange.bybit_testnet,
                rest_url: None,
                max_leverage: 100,
                enabled: true,
            };
            let core_id = topology.ws_bybit_core;
            let handle = thread::Builder::new()
                .name("ws-bybit".into())
                .spawn(move || {
                    let _ = core_affinity::set_for_current(
                        core_affinity::CoreId { id: core_id }
                    );
                    info!("[ws-bybit] Pinned to core {}", core_id);
                    let rt = tokio::runtime::Builder::new_current_thread()
                        .enable_all()
                        .build()
                        .expect("Failed to build tokio runtime for ws-bybit");
                    rt.block_on(
                        multi_exchange::ws_ingestion_multi::run_bybit_ws_ingestion(
                            bybit_cfg, reg, sym_reg,
                        )
                    );
                })
                .expect("Failed to spawn Bybit WS thread");
            thread_handles.push(handle);
        }
    }
}
Pass global_book_registry, binance_gateway, bybit_gateway, and
multi_exchange_gateways into execution_router_loop() and
strategy_evaluator_loop() as additional parameters.

In strategy_evaluator_loop(), when multi_exchange_enabled is true, read
from the GlobalBookRegistry to get cross-exchange spread data and pass it
to the strategy engine as additional microstructure metrics.

In execution_router_loop(), when multi_exchange_enabled is true:

Use the SmartOrderRouter to calculate optimal routing for each order
Execute order slices in parallel using tokio::join! across all exchanges
Spawn a background task every 30 seconds to call
CrossVenueMarginMonitor::refresh_all() and log margin health
Spawn a background task every 60 seconds to call
CrossExchangeFundingArb::scan_opportunities() and log opportunities



──────────────────────────────────────────────────────────────────────────────────────
MODIFY: rust_engine/src/dashboard_server.rs
──────────────────────────────────────────────────────────────────────────────────────

Add new fields to DashboardState for multi-exchange data:
// ── Multi-Exchange State ──────────────────────────────────────────────────
/// Whether multi-exchange mode is active.
pub multi_exchange_enabled: AtomicBool,
/// Per-exchange balance (Gate.io=0, Binance=1, Bybit=2) in USDT × 1e8.
pub exchange_balances: [AtomicI64; 3],
/// Per-exchange margin ratio × 10000 (e.g. 7500 = 75.00%).
pub exchange_margin_ratios: [AtomicI64; 3],
/// JSON-encoded multi-exchange positions (includes exchange field per position).
multi_exchange_positions_json: std::sync::RwLock<String>,
/// JSON-encoded funding rate arbitrage opportunities.
funding_arb_json: std::sync::RwLock<String>,
/// JSON-encoded global book snapshot (best bid/ask per exchange per symbol).
global_book_json: std::sync::RwLock<String>,
Add setter methods for the new fields:
pub fn set_exchange_balance(&self, exchange_idx: usize, balance: f64) {
    if exchange_idx < 3 {
        self.exchange_balances[exchange_idx].store((balance * 1e8) as i64, Ordering::Relaxed);
    }
}

pub fn set_exchange_margin_ratio(&self, exchange_idx: usize, ratio: f64) {
    if exchange_idx < 3 {
        self.exchange_margin_ratios[exchange_idx].store((ratio * 10000.0) as i64, Ordering::Relaxed);
    }
}

pub fn set_multi_exchange_positions_json(&self, json: String) {
    if let Ok(mut guard) = self.multi_exchange_positions_json.write() {
        *guard = json;
    }
}

pub fn set_funding_arb_json(&self, json: String) {
    if let Ok(mut guard) = self.funding_arb_json.write() {
        *guard = json;
    }
}

pub fn set_global_book_json(&self, json: String) {
    if let Ok(mut guard) = self.global_book_json.write() {
        *guard = json;
    }
}
Update to_json() to include multi-exchange data when enabled:
Add "multi_exchange": { "enabled": ..., "exchange_balances": [...], "exchange_margin_ratios": [...], "positions": ..., "funding_arb": ..., "global_book": ... } to the JSON output.

Update template_context() to add:

use_multi_exchange: bool from multi_exchange_enabled atomic
exchange_balances: array of 3 f64 values
exchange_margin_ratios: array of 3 f64 values
max_open_positions: 5 if multi_exchange_enabled, else 3
multi_exchange_positions: parsed JSON Value
funding_arb_opportunities: parsed JSON Value
global_book: parsed JSON Value


Add new API endpoints to the axum router:




Insert at cursor
// Multi-Exchange API endpoints
.route("/multi-exchange", get(page_multi_exchange))
.route("/api/multi-exchange/portfolio", get(api_multi_exchange_portfolio))
.route("/api/multi-exchange/positions", get(api_multi_exchange_positions))
.route("/api/multi-exchange/funding-arb", get(api_multi_exchange_funding_arb))
.route("/api/multi-exchange/global-book", get(api_multi_exchange_global_book))
.route("/api/multi-exchange/sor-preview", get(api_multi_exchange_sor_preview))
.route("/api/multi-exchange/margin-health", get(api_multi_exchange_margin_health))
.route("/api/multi-exchange/rebalance", axum::routing::post(api_multi_exchange_rebalance))
.route("/api/positions/:exchange/:symbol/close",
       axum::routing::post(api_close_position_on_exchange))


Implement all new handler functions:



Insert at cursor
async fn page_multi_exchange(State(state): State<Arc<AppState>>) -> impl IntoResponse {
    let mut ctx = state.dashboard.template_context("multi_exchange");
    // Add multi-exchange specific context
    ctx.insert("use_multi_exchange", &state.dashboard.multi_exchange_enabled.load(Ordering::Relaxed));
    render_template(&state.templates, "multi_exchange.html", &ctx)
}

async fn api_multi_exchange_portfolio(State(state): State<Arc<AppState>>) -> Json<Value> {
    let d = &state.dashboard;
    let balances: Vec<f64> = (0..3)
        .map(|i| d.exchange_balances[i].load(Ordering::Relaxed) as f64 / 1e8)
        .collect();
    let margin_ratios: Vec<f64> = (0..3)
        .map(|i| d.exchange_margin_ratios[i].load(Ordering::Relaxed) as f64 / 10000.0)
        .collect();
    let total_balance: f64 = balances.iter().sum();

    Json(json!({
        "success": true,
        "total_balance": total_balance,
        "total_equity": d.equity(),
        "total_unrealized_pnl": d.unrealized_pnl(),
        "exchanges": [
            {
                "name": "Gate.io",
                "id": "gateio",
                "balance": balances[0],
                "margin_ratio": margin_ratios[0],
                "color": "primary",
                "is_healthy": margin_ratios[0] > 0.3,
            },
            {
                "name": "Binance",
                "id": "binance",
                "balance": balances[1],
                "margin_ratio": margin_ratios[1],
                "color": "warning",
                "is_healthy": margin_ratios[1] > 0.3,
            },
            {
                "name": "Bybit",
                "id": "bybit",
                "balance": balances[2],
                "margin_ratio": margin_ratios[2],
                "color": "danger",
                "is_healthy": margin_ratios[2] > 0.3,
            },
        ],
        "multi_exchange_enabled": d.multi_exchange_enabled.load(Ordering::Relaxed),
    }))
}

async fn api_multi_exchange_positions(State(state): State<Arc<AppState>>) -> impl IntoResponse {
    let raw = state.dashboard.multi_exchange_positions_json
        .read()
        .map(|g| g.clone())
        .unwrap_or_else(|_| "[]".to_string());
    let positions: Value = serde_json::from_str(&raw).unwrap_or(Value::Array(vec![]));
    Json(json!({ "success": true, "positions": positions }))
}

async fn api_multi_exchange_funding_arb(State(state): State<Arc<AppState>>) -> impl IntoResponse {
    let raw = state.dashboard.funding_arb_json
        .read()
        .map(|g| g.clone())
        .unwrap_or_else(|_| "[]".to_string());
    let opportunities: Value = serde_json::from_str(&raw).unwrap_or(Value::Array(vec![]));
    Json(json!({ "success": true, "opportunities": opportunities }))
}

async fn api_multi_exchange_global_book(State(state): State<Arc<AppState>>) -> impl IntoResponse {
    let raw = state.dashboard.global_book_json
        .read()
        .map(|g| g.clone())
        .unwrap_or_else(|_| "{}".to_string());
    (StatusCode::OK, [(axum::http::header::CONTENT_TYPE, "application/json")], raw)
}

#[derive(Deserialize)]
struct SorPreviewQuery {
    symbol: String,
    side: String,
    size_usdt: f64,
}

async fn api_multi_exchange_sor_preview(
    Query(params): Query<SorPreviewQuery>,
    State(state): State<Arc<AppState>>,
) -> Json<Value> {
    // Return a preview of how the SOR would split the order
    // In production this reads from the GlobalBookRegistry
    // For now return a structured response showing the routing logic
    Json(json!({
        "success": true,
        "symbol": params.symbol,
        "side": params.side,
        "total_size_usdt": params.size_usdt,
        "routing": "single_exchange",
        "slices": [
            {
                "exchange": "Gate.io",
                "size_usdt": params.size_usdt,
                "expected_cost_bps": 5.0,
                "is_maker": false,
            }
        ],
        "estimated_slippage_bps": 5.0,
        "message": "SOR preview — connect GlobalBookRegistry for live routing",
    }))
}

async fn api_multi_exchange_margin_health(State(state): State<Arc<AppState>>) -> Json<Value> {
    let d = &state.dashboard;
    let exchanges = ["gateio", "binance", "bybit"];
    let names = ["Gate.io", "Binance", "Bybit"];
    let health: Vec<Value> = (0..3).map(|i| {
        let balance = d.exchange_balances[i].load(Ordering::Relaxed) as f64 / 1e8;
        let ratio = d.exchange_margin_ratios[i].load(Ordering::Relaxed) as f64 / 10000.0;
        json!({
            "exchange": exchanges[i],
            "name": names[i],
            "balance": balance,
            "margin_ratio": ratio,
            "status": if ratio > 0.5 { "healthy" } else if ratio > 0.3 { "warning" } else { "critical" },
        })
    }).collect();

    Json(json!({ "success": true, "health": health }))
}

async fn api_multi_exchange_rebalance() -> Json<Value> {
    Json(json!({
        "success": true,
        "message": "Rebalance request acknowledged — manual transfer required",
    }))
}

async fn api_close_position_on_exchange(
    Path((exchange, symbol)): Path<(String, String)>,
) -> Json<Value> {
    Json(json!({
        "success": true,
        "message": format!("Close request for {} on {} acknowledged", symbol, exchange),
    }))
}


Update spawn_ws_broadcast_task() to include multi-exchange data in the
WebSocket broadcast payload:



Insert at cursor
// Add to the payload json!({...}):
"multi_exchange": {
    "enabled": state.multi_exchange_enabled.load(Ordering::Relaxed),
    "exchange_balances": [
        state.exchange_balances[0].load(Ordering::Relaxed) as f64 / 1e8,
        state.exchange_balances[1].load(Ordering::Relaxed) as f64 / 1e8,
        state.exchange_balances[2].load(Ordering::Relaxed) as f64 / 1e8,
    ],
    "exchange_margin_ratios": [
        state.exchange_margin_ratios[0].load(Ordering::Relaxed) as f64 / 10000.0,
        state.exchange_margin_ratios[1].load(Ordering::Relaxed) as f64 / 10000.0,
        state.exchange_margin_ratios[2].load(Ordering::Relaxed) as f64 / 10000.0,
    ],
},

──────────────────────────────────────────────────────────────────────────────────────
MODIFY: crypto_trading_bot/.env.example
──────────────────────────────────────────────────────────────────────────────────────
Add the following section AFTER the existing # ── Feature 5: Multi-Exchange Arbitrage ──
section and BEFORE the # ── Feature 6: Fee Optimization ── section:


Insert at cursor
# ============ MULTI-EXCHANGE FEATURE (USE_MULTI_EXCHANGE) ============
# Master toggle for the full multi-exchange / cross-exchange feature.
# When set to "on", the Rust engine initializes simultaneous WebSocket
# connections to Gate.io, Binance, AND Bybit, fuses their order books
# into a Consolidated Global Order Book, and enables:
#   - Smart Order Routing (SOR) across all three exchanges
#   - Cross-exchange funding rate arbitrage detection
#   - Cross-venue margin health monitoring
#   - Delta-neutral position management
#   - Max open positions increased from 3 to 5
#
# When set to "off" (default), the bot runs in EXACT single-exchange mode
# (Gate.io only) with ZERO performance impact or behavioral change.
#
# Accepted values: on/off, true/false, yes/no, 1/0 (case-insensitive)
USE_MULTI_EXCHANGE=off

# ── Binance Futures credentials (required when USE_MULTI_EXCHANGE=on) ──
# Generate at: https://www.binance.com/en/my/settings/api-management
# Ensure "Futures" permission is enabled.
# For testnet: https://testnet.binancefuture.com/
BINANCE_API_KEY=your_binance_api_key
BINANCE_SECRET_KEY=your_binance_secret_key
# Set to true to use Binance Futures testnet (https://testnet.binancefuture.com)
BINANCE_TESTNET=false

# ── Bybit v5 credentials (required when USE_MULTI_EXCHANGE=on) ──
# Generate at: https://www.bybit.com/app/user/api-management
# Ensure "Derivatives" / "USDT Perpetual" permission is enabled.
# For testnet: https://testnet.bybit.com/
BYBIT_API_KEY=your_bybit_api_key
BYBIT_SECRET_KEY=your_bybit_secret_key
# Set to true to use Bybit testnet (https://api-testnet.bybit.com)
BYBIT_TESTNET=false

# ── Multi-Exchange Live vs. Testnet Matrix ──────────────────────────────────
# The following table shows which endpoints are used for each combination:
#
# TRADING_MODE  | BINANCE_TESTNET | Binance REST/WS Endpoint
# --------------|-----------------|------------------------------------------
# live/paper    | false           | fapi.binance.com / fstream.binance.com
# testnet       | true            | testnet.binancefuture.com / stream.binancefuture.com
#
# TRADING_MODE  | BYBIT_TESTNET   | Bybit REST/WS Endpoint
# --------------|-----------------|------------------------------------------
# live/paper    | false           | api.bybit.com / stream.bybit.com
# testnet       | true            | api-testnet.bybit.com / stream-testnet.bybit.com
#
# NOTE: Gate.io testnet is controlled by TRADING_MODE=testnet (existing behavior).
# Binance and Bybit testnet are controlled by their own BINANCE_TESTNET /
# BYBIT_TESTNET flags, independent of TRADING_MODE.

# ── Smart Order Router (SOR) configuration ─────────────────────────────────
# Minimum order size in USDT to trigger cross-exchange splitting (default: 5000)
# SOR_MIN_SPLIT_SIZE_USDT=5000
# Maximum number of exchanges to split across (1-3, default: 3)
# SOR_MAX_VENUES=3
# Maximum acceptable slippage in basis points (default: 30)
# SOR_MAX_SLIPPAGE_BPS=30

# ── Funding Rate Arbitrage configuration ───────────────────────────────────
# Minimum net funding rate spread to consider actionable (default: 0.005%)
# FUNDING_ARB_MIN_NET_RATE=0.00005
# Minimum annualized APR to consider actionable (default: 10%)
# FUNDING_ARB_MIN_APR=10.0

# ── Cross-Venue Margin Monitor configuration ───────────────────────────────
# Minimum acceptable margin ratio before alert (default: 30%)
# MARGIN_MONITOR_MIN_RATIO=0.30
# Critical margin ratio threshold (default: 15%)
# MARGIN_MONITOR_CRITICAL_RATIO=0.15

──────────────────────────────────────────────────────────────────────────────────────
MODIFY: rust_engine/Dockerfile
──────────────────────────────────────────────────────────────────────────────────────
Replace the existing Dockerfile with the following complete version:


Insert at cursor
# Stage 1: Build
FROM rust:1.85-slim-bookworm AS builder

RUN apt-get update && apt-get install -y \
    pkg-config libssl-dev cmake python3 \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app/rust_engine
COPY rust_engine/Cargo.toml rust_engine/Cargo.lock ./
COPY rust_engine/src/ src/

# Build with maximum optimization
ENV RUSTFLAGS="-C target-cpu=x86-64-v3"
RUN cargo build --release --bin trading_engine

# Stage 2: Runtime (minimal)
FROM debian:bookworm-slim

RUN apt-get update && apt-get install -y \
    ca-certificates libssl3 \
    && rm -rf /var/lib/apt/lists/*

COPY --from=builder /app/rust_engine/target/release/trading_engine /usr/local/bin/

# Copy templates and static files for the Rust dashboard server
COPY crypto_trading_bot/templates/ /app/templates/
COPY crypto_trading_bot/static/ /app/static/

# ── Port Exposure ──────────────────────────────────────────────────────────
# 8080: Web dashboard (HTTP + WebSocket)
# 5555: ZeroMQ telemetry PUB (Rust → Python)
# 5556: ZeroMQ config PULL (Python → Rust)
# 9090: Multi-exchange metrics endpoint (Prometheus-compatible, future use)
EXPOSE 8080 5555 5556 9090

# ── Shared Memory Directories ──────────────────────────────────────────────
# /dev/shm is available by default in Docker. The following paths are used:
#   /dev/shm/regime_weights     — Python → Rust regime state (seqlock)
#   /dev/shm/ml_weights         — Python → Rust ML weights (zero-copy)
#   /dev/shm/gamma_exposure     — Python → Rust gamma flip levels
#   /dev/shm/engine_telemetry   — Rust → Python telemetry
#   /dev/shm/multi_exchange_state — Rust → Python multi-exchange state (NEW)
# These are created at runtime by the engine; no Docker volume needed.

# ── Environment Variable Defaults ─────────────────────────────────────────
# These can be overridden by docker-compose.yml or docker run -e flags.
ENV TRADING_MODE=paper \
    USE_MULTI_EXCHANGE=off \
    DASHBOARD_PORT=8080 \
    LOG_LEVEL=INFO \
    THREAD_TOPOLOGY=auto

# Health check via heartbeat file (written every 500ms by telemetry thread)
HEALTHCHECK --interval=5s --timeout=3s --retries=3 \
    CMD test -f /tmp/engine_heartbeat || exit 1

ENTRYPOINT ["trading_engine"]
CMD ["--config", "/etc/trading/engine_config.toml"]

═══════════════════════════════════════════════════════════════════════════════════════
SECTION 4: LIVE vs. TESTNET ENDPOINT MATRIX — MANDATORY IMPLEMENTATION
═══════════════════════════════════════════════════════════════════════════════════════
You MUST implement the following endpoint selection logic in every gateway and
WS ingestion function. No hardcoded URLs anywhere except in the constants defined
at the top of each file.
GATE.IO:
  Live REST:    https://api.gateio.ws/api/v4
  Live WS:      wss://ws.gate.com/v4/ws/futures/usdt
  Testnet REST: https://api-testnet.gateapi.io/api/v4
  Testnet WS:   wss://ws-testnet.gate.com/v4/ws/futures/usdt
  Trigger:      TRADING_MODE=testnet OR ExchangeConfig.testnet=true
BINANCE FUTURES:
  Live REST:    https://fapi.binance.com
  Live WS:      wss://fstream.binance.com/stream
  Testnet REST: https://testnet.binancefuture.com
  Testnet WS:   wss://stream.binancefuture.com/stream
  Trigger:      BINANCE_TESTNET=true OR ExchangeConfig.testnet=true
BYBIT v5:
  Live REST:    https://api.bybit.com
  Live WS:      wss://stream.bybit.com/v5/public/linear
  Testnet REST: https://api-testnet.bybit.com
  Testnet WS:   wss://stream-testnet.bybit.com/v5/public/linear
  Trigger:      BYBIT_TESTNET=true OR ExchangeConfig.testnet=true
AUTHENTICATION DIFFERENCES:
  Gate.io:  HMAC-SHA512, headers: KEY, SIGN, Timestamp
  Binance:  HMAC-SHA256, query param: signature=..., header: X-MBX-APIKEY
  Bybit v5: HMAC-SHA256, headers: X-BAPI-API-KEY, X-BAPI-SIGN,
            X-BAPI-TIMESTAMP, X-BAPI-RECV-WINDOW
These are already correctly implemented in the existing gateway files.
The new multi-exchange WS ingestion (ws_ingestion_multi.rs) handles
PUBLIC WebSocket streams only (no authentication required for orderbook data).
═══════════════════════════════════════════════════════════════════════════════════════
SECTION 5: POSITION LIMIT ENFORCEMENT
═══════════════════════════════════════════════════════════════════════════════════════
The PositionSlotManager in position_slot_manager.rs enforces the hard position limit.
Currently initialized with config.risk.max_open_positions (default: 3).
When USE_MULTI_EXCHANGE=on:

config.risk.max_open_positions is overridden to 5 in apply_env_overrides()
The PositionSlotManager is initialized with 5 slots
The dashboard displays "Max: 5" in the position limit indicator
The template_context() returns max_open_positions=5

When USE_MULTI_EXCHANGE=off:

config.risk.max_open_positions remains at its configured value (default: 3)
The PositionSlotManager is initialized with 3 slots
The dashboard displays "Max: 3"
ZERO change to existing behavior

The position limit is enforced in strategy_evaluator_loop() via:
  if !position_slots.try_acquire() { continue; }
This code is UNCHANGED. Only the initialization value changes.
═══════════════════════════════════════════════════════════════════════════════════════
SECTION 6: DASHBOARD MULTI-EXCHANGE FEATURES — COMPLETE SPECIFICATION
═══════════════════════════════════════════════════════════════════════════════════════
The multi_exchange.html template must implement ALL of the following features
using real data from the API endpoints defined in Section 3:

MULTI-EXCHANGE PORTFOLIO SUMMARY

Fetches from GET /api/multi-exchange/portfolio on page load
Auto-refreshes every 5 seconds via WebSocket or polling
Shows total equity = sum of all exchange balances + unrealized PnL
Per-exchange balance cards with exchange-specific colors:
Gate.io: Bootstrap "primary" (blue)
Binance: Bootstrap "warning" (yellow)
Bybit:   Bootstrap "danger" (orange/red)
Margin health bar per exchange (Bootstrap progress bar, color-coded)


ACTIVE TRADES WITH EXCHANGE TAGS

Fetches from GET /api/multi-exchange/positions
Each trade row shows an exchange badge (colored pill)
Badge text: "Gate.io", "Binance", or "Bybit"
Badge color matches the exchange color scheme above
Maximum 5 rows (enforced by MAX_OPEN_POSITIONS=5)
"Close" button per row calls POST /api/positions/{exchange}/{symbol}/close
Columns: Exchange, Symbol, Direction (Long/Short), Size, Entry, Current,
Unrealized PnL ($), PnL (%), Leverage, SL, TP, Duration, Close


FUNDING RATE ARBITRAGE PANEL

Fetches from GET /api/multi-exchange/funding-arb
Table with columns: Symbol, Gate.io Rate, Binance Rate, Bybit Rate,
Best Spread, Annualized APR, Action
Rates displayed as percentages with 4 decimal places (e.g. "0.0100%")
Best Spread column: green text when spread > 0.01%, red when negative
"Open Arb" button when annualized APR > 10%
Auto-refreshes every 60 seconds


GLOBAL ORDER BOOK PANEL

Fetches from GET /api/multi-exchange/global-book
Three-column layout: Gate.io | Binance | Bybit
Each column shows: Best Bid, Best Ask, Spread (bps)
Globally best bid highlighted with ★ icon (green)
Globally best ask highlighted with ★ icon (red)
Fee-adjusted effective prices shown in parentheses
Auto-refreshes every 1 second


SMART ORDER ROUTER PREVIEW

Form with: Symbol (dropdown from TRADING_PAIRS), Side (Buy/Sell),
Size in USDT (number input)
"Preview Route" button calls GET /api/multi-exchange/sor-preview
Results table: Exchange, Size (USDT), Size (contracts), Expected Cost (bps),
Order Type (Maker/Taker)
Shows "Estimated Savings vs. Single Exchange: X bps"


MARGIN HEALTH PANEL

Fetches from GET /api/multi-exchange/margin-health
Three gauge charts (Bootstrap progress bars) for each exchange
Color coding: > 50% = green, 30-50% = yellow, < 30% = red
Alert banner (Bootstrap alert-danger) when any exchange < 30%
"Rebalance" button calls POST /api/multi-exchange/rebalance


REAL-TIME WEBSOCKET INTEGRATION

JavaScript connects to ws://host:8080/ws/live
Handles message type "update" with "multi_exchange" field
Updates exchange balance cards, margin ratios, and position table
Shows "Last Updated: X seconds ago" timestamp
Reconnects automatically on disconnect with 3-second backoff


NAVIGATION

Add "Multi-Exchange" link to the sidebar in base.html
Only visible when use_multi_exchange=true in template context
Active state when current page is "multi_exchange"



═══════════════════════════════════════════════════════════════════════════════════════
SECTION 7: CARGO.TOML — NO CHANGES REQUIRED
═══════════════════════════════════════════════════════════════════════════════════════
The existing Cargo.toml already has all required dependencies:

tokio-tungstenite (WebSocket)
reqwest (HTTP client)
serde / serde_json (serialization)
dashmap (concurrent hashmap)
parking_lot (RwLock)
hmac / sha2 / hex (authentication)
axum (HTTP server)
tracing (logging)

DO NOT add new dependencies. All new code must use only existing crate imports.
═══════════════════════════════════════════════════════════════════════════════════════
SECTION 8: COMPILATION REQUIREMENTS
═══════════════════════════════════════════════════════════════════════════════════════
The following must be true after your implementation:

cargo build --release in rust_engine/ must succeed with ZERO errors.
cargo clippy -- -D warnings must produce ZERO warnings.
When USE_MULTI_EXCHANGE=off, the binary behavior is IDENTICAL to the
current codebase. No extra threads. No extra allocations. No altered logic.
When USE_MULTI_EXCHANGE=on, all three exchange WS connections are established,
the GlobalBookRegistry is populated, and the dashboard shows multi-exchange data.
All new Rust code must use #[allow(dead_code)] ONLY for fields that are
intentionally unused in the current implementation phase. Do NOT suppress
warnings for logic errors.
All new async functions must be properly awaited. No fire-and-forget without
explicit tokio::spawn().
All new structs that are passed across thread boundaries must implement
Send + Sync (or be wrapped in Arc<>).

═══════════════════════════════════════════════════════════════════════════════════════
SECTION 9: EXECUTION ORDER
═══════════════════════════════════════════════════════════════════════════════════════
Implement in this exact order to avoid compilation errors:

rust_engine/src/multi_exchange/mod.rs (module declarations only)
rust_engine/src/multi_exchange/global_book.rs (no dependencies on other new files)
rust_engine/src/multi_exchange/sor.rs (depends on global_book)
rust_engine/src/multi_exchange/funding_arb.rs (depends on global_book)
rust_engine/src/multi_exchange/margin_monitor.rs (depends on global_book)
rust_engine/src/multi_exchange/ws_ingestion_multi.rs (depends on global_book)
rust_engine/src/config.rs (add MultiExchangeConfig, ThreadTopology fields)
rust_engine/src/dashboard_server.rs (add new fields and endpoints)
rust_engine/src/main.rs (wire everything together)
crypto_trading_bot/templates/multi_exchange.html (new template)
crypto_trading_bot/templates/dashboard.html (modify existing)
crypto_trading_bot/.env.example (add new variables)
rust_engine/Dockerfile (update)

═══════════════════════════════════════════════════════════════════════════════════════
SECTION 10: FINAL VERIFICATION CHECKLIST
═══════════════════════════════════════════════════════════════════════════════════════
Before submitting your implementation, verify:
[ ] USE_MULTI_EXCHANGE=off → cargo build succeeds, behavior identical to current
[ ] USE_MULTI_EXCHANGE=on  → cargo build succeeds, 3 WS threads spawned
[ ] BINANCE_TESTNET=true   → Binance uses testnet.binancefuture.com endpoints
[ ] BYBIT_TESTNET=true     → Bybit uses api-testnet.bybit.com endpoints
[ ] MAX_OPEN_POSITIONS=5   → when USE_MULTI_EXCHANGE=on
[ ] MAX_OPEN_POSITIONS=3   → when USE_MULTI_EXCHANGE=off (default)
[ ] /multi-exchange page   → renders without template errors
[ ] /api/multi-exchange/*  → all endpoints return valid JSON
[ ] Exchange badges         → Gate.io=blue, Binance=yellow, Bybit=orange
[ ] .env.example           → documents all new variables with comments
[ ] Dockerfile             → exposes port 9090, sets USE_MULTI_EXCHANGE=off default
[ ] No pseudo-code         → every function has a complete implementation
[ ] No TODO comments       → all logic is implemented, not deferred
[ ] No placeholder returns → no unimplemented!() or todo!() macros
═══════════════════════════════════════════════════════════════════════════════════════
END OF MASTER PROMPT
═══════════════════════════════════════════════════════════════════════════════════════
```

Hey  understand the current situation of the repository completey . Your task is to search on web ( complete documentations of bybit , binance and gate io )  to fix all the errors with proper approach and with proper working code ( no fake or pseudo code ) . Nextly again use your full knowledge to add all the gaps or features that bot is lacking as mentioned in bugs.txt properly by a deep research. No hardcoded values should be there . Evrything should be fetched in real time from exchange. Also search on web how to avoid maker and taker fees in finding arbitrage trading and add the same logic ( with complete working code ) to my bot. Below are all the errors or the bugs or features gap that bot is having.

Note : Here is what is done from one one of your previous session.

Devin's execution plan

[x] Explore repository structure and understand codebase

[x] Research Bybit, Binance, Gate.io API docs for precision rules, WebSocket format, and fee structures

[x] Checkout a new git branch for changes

[x] Bug Fix #1: Fix Gate.io WebSocket req_id placement - move from nested payload to root level

[x] Feature: Build unified InstrumentManager module that fetches contract specs from all 3 exchanges at startup

[x] Bug Fix #2 & #3: Integrate InstrumentManager into Binance/Bybit gateways for proper price/qty formatting

[x] Add helper functions (exchange_id_to_exchange, get_approximate_price, get_or_default) and complete integration

[ ] Commit current changes and push to remote

[ ] Create PR with all fixes and features

[ ] Wait for cargo check / fix compilation errors

[ ] Feature: Add symbol normalization with contract multiplier conversion at gateway level

[ ] Feature: Add pre-flight margin/risk simulation before order submission

[ ] Feature: Enhance fee optimization - add Bybit fee tiers, BNB discount logic, post-only enforcement across all exchanges

[ ] Integrate InstrumentManager into Gate.io gateway for price formatting

[ ] Integrate InstrumentManager + fee strategy into funding arb engine/executor

[ ] Feature: Add execution idempotency - REST reconciliation before retrying timed-out orders

[ ] Audit and remove all remaining hardcoded values

[ ] Wait for CI checks to pass

[ ] Update environment config for future sessions

[ ] Report results to user and suggest testing the app end-to-end

(x) represent that is done ( tick mark ).




1. Gate.io Manual Trade & Auto Trade Failures (Timeout)
The Error in Logs: Timeout waiting for order r1 ACK (5000ms) followed by Ghost tracking entry removed. The Root Cause: The issue lies in how your Rust engine constructs the Gate.io WebSocket message for futures.order_place. You have nested the req_id field inside the "payload" JSON object. However, the Gate.io WebSocket API V4 requires req_id to be at the root level of the JSON request. Because it is incorrectly nested, Gate.io does not echo the req_id back in its response. Your Rust engine's ws_ingestion thread receives the exchange's response but cannot match it to the pending order map, resulting in the 5-second timeout, multiple retries, and eventually discarding it as a "ghost" order. Because manual trades share this execution gateway, auto-trades on Gate.io fail for the exact same reason.

2. Funding Arbitrage Errors (InvalidPrice & InsufficientBalance)
The Error in Logs: Entry failed on both legs: short=InvalidPrice long=InsufficientBalance for BTC_USDT with size=1. The Root Causes:

Insufficient Balance (Binance Long): Your logs show a Binance balance of $4943.14. You are using 2x leverage, making your total purchasing power ~$9,886. The bot is trying to place an order with size=1 for BTC_USDT. On Binance's Linear USDT-M futures, size=1 equals 1 whole Bitcoin (valued at roughly $60k-$90k depending on current market price). Because $9,886 is nowhere near enough to buy 1 full BTC, Binance correctly rejects the order for Insufficient Balance. Your bot's sizing logic is failing to calculate fractional position sizes (e.g., 0.05 BTC) based on available equity.
Invalid Price (Bybit Short): Bybit error 110007 corresponds to InvalidPrice. This happens because your execution engine stringifies prices uniformly (e.g., forcibly formatting to 8 decimals "{:.8}") without respecting Bybit's specific tickSize and price scale requirements for BTCUSDT. Alternatively, if this was supposed to be a market order, passing an improperly formatted or out-of-band limit price triggers Bybit's price protection limits.
3. Missing Institutional-Level Logic & Gaps
Comparing your current architecture to a profitable, top-tier institutional bot, here are the main logic flaws and missing features:

Contract Multiplier & Symbol Normalization: Your system treats size=1 identically across all exchanges. In reality, exchanges have completely different contract multipliers. For example, 1 contract on Gate.io might represent 1 USD or 1 multiplier of an asset, while on Bybit/Binance 1 contract might literally mean 1 BTC. Institutional bots normalize "Quantity" into base asset values (e.g., Notional USD) and convert them back to exchange-specific contract sizes at the gateway level.
Exchange Precision Rules (Lot Size / Tick Size): Exchanges will strictly reject orders if the price doesn't align with their tickSize (e.g., Bybit might require 0.5 increments for BTC) or if the quantity doesn't match the stepSize. The current codebase lacks a dynamic instrument manager that enforces strict rounding/truncation prior to order submission.
Execution Idempotency & Reconciliations: When the WebSocket ACK times out, your system blindly retries the submission. In a real-world scenario (or live mode), the exchange might have actually accepted the first order, but the WS ACK was just delayed. Retrying 5 times without verifying via a REST GET check can lead to you accidentally opening 5x the intended position size. Institutional systems use strict FIX/REST reconciliation before assuming an order truly failed.
Risk Overrides on Execution: Your multi_exchange::funding_arb_engine checks the basis risk (which rejected some trades in the logs), but it pushes the size limits without checking the actual margin impact before calling the gateway. Institutional engines simulate the margin requirement of the legs against the specific exchange's leverage/tier limits locally to prevent "Insufficient Balance" rejections in-flight, which can leave you "legged in" (one side fills, the other side fails).
